# ADR 索引

手工维护：新增 ADR 时追加一行，编号取现有最大号 + 1（ADR 0017）。

| 编号 | 标题 | 日期 | 改写了哪几份 | 被哪几份改写 |
| --- | --- | --- | --- | --- |
| 0002 | tracker 与仓库文件的权威归属 | 2026-08-11 | 无 | 无 |
| 0012 | 界面 QA 绑设计系统的格式规范，不绑生成它的工具 | 2026-08-13 | 无 | 0016 |
| 0015 | MMW 不打包成插件，五个宿主由 `install.sh` 统一散装 | 2026-08-18 | 无 | 0018 |
| 0016 | 设计系统文件可不可信，由校验结果定，不由来源定 | 2026-08-21 | 0012 | 无 |
| 0017 | docs 文档层过继到 v2：tracker 配置落地，索引与编号脱离冻结 CLI | 2026-08-25 | 无 | 无 |
| 0018 | 技能装进一个各家通用的位置，只为 Claude Code 单独再装一份 | 2026-08-26 | 0015 | 无 |

编号不连续是有意的。删掉的编号不回收，也不重编——重编一次就要多维护一层旧号翻译。

## 编号在本仓库以外仍会出现

issue tracker、commit message 和 `docs/plans/`、`docs/research/` 里的历史材料仍写着旧编号，而**同一个号在不同时期指的不是同一份**。遇到本目录以外的 ADR 编号，先按这张表翻译：

| 历史上写过的编号 | 现在 |
| --- | --- |
| 0002 | 0002，未变 |
| 0015 | 0012 |
| 0018 | 0015 |
| 0019 | 0016 |
| 0023 | 0017 |
| 0024 | 0018 |
| 其余任何编号 | 已删除，没有对应 |

## 删过哪几批

- **2026-08-26，12 份。** 主题依附上一代 `mmw` CLI（`mmw artifact path`、`mmw task`、`mmw domain`）与 `mmw/skills-src/`，两者都已进 `archive/`，活层零调用；产物落点合同、产物引用、索引由命令算出、机械校验的两条正则、界面 QA 依赖装进 runtime 这几条，在 v2 里都没有承接对象。同批删掉 `docs/specs/` 与 `docs/assets/`。
- **2026-08-26 之前，两批。** 一批是重编号时并掉的三份，一批是落地后被整批撤回的三份。两批都没有对应文件。
