# docs/research

调研产物。Wayfinder 决策工单的调研走 `issue-<编号>` 子层；其余调研直接用 `<名字段>/<主题>/`。

## 关键约定

- 路径 `docs/research/<名字段>/issue-<编号>/<主题>/`，拆成 `README.md`（索引）和 `report.md`（正文）；`issue-<编号>` 这一层只有 Wayfinder 决策工单有。
- `README.md` 带「章节指引」一节，说 `report.md` 每节讲什么；不点名决策工单编号，不说该读哪几节。
