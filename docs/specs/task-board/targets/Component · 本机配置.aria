# target trees of the handoff package page: Component · 本机配置.dc.html
# derived by extract_skeleton.py — the handoff package is the baseline and this file is its readable view; the lint fails when the hashes below no longer match the package
# scenes.json sha256=5873450cb918666f5b2f7919883486055f1ada43e45589bacca22d1e3a93be5f
# page sha256=aad4005dd1763d1b1c2dedb8709b2ac39f4cf2bf22a0bcc0d436293bf323b3bc

## scene mine
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" < combobox "senior-worker 的 host"
- option "codex" [selected] < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model"
- option "gpt 5.6 luna" < combobox "senior-worker 的 model"
- option "gpt 5.5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- option "ultra" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 没有改动 上面就是下一个新起的 agent 会用的配置。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene fresh
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 本机没装 claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok · 本机没装" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" [disabled] < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model" [disabled]
- combobox "junior-worker 的 effort" [disabled] < dialog "本机配置"
- option "high" [selected] < combobox "junior-worker 的 effort" [disabled]
- text: 这台机器上没装 grok 的 CLI（grok），换一个这台机器有的 senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok · 本机没装" [disabled] < combobox "senior-worker 的 host"
- option "claude" < combobox "senior-worker 的 host"
- option "codex" [selected] < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model"
- option "gpt 5.6 luna" < combobox "senior-worker 的 model"
- option "gpt 5.5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- option "ultra" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok · 本机没装" [disabled] < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok · 本机没装" [disabled] < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok · 本机没装" [disabled] < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 有 1 处 start 会拒绝，改好之前不能保存 带斜线的格子，下面一行写着哪里不对。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene retired
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:30 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 3 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" < combobox "senior-worker 的 host"
- option "codex" [selected] < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model"
- option "gpt 5.6 luna" < combobox "senior-worker 的 model"
- option "gpt 5.5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- option "ultra" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "fable 5 · 本机已经没有" [selected] < combobox "advisor 的 model"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" < combobox "advisor 的 model"
- combobox "advisor 的 effort" [disabled] < dialog "本机配置"
- option "medium" [selected] < combobox "advisor 的 effort" [disabled]
- text: 这台机器的 claude 已经不提供 fable 5 < dialog "本机配置"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 有 1 处 start 会拒绝，改好之前不能保存 带斜线的格子，下面一行写着哪里不对。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene paseo-off
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问 Paseo · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor Paseo 没开 grok Paseo 没开 claude Paseo 没开 codex Paseo 没开 pi Paseo 没开 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" < combobox "runner"
- option "paseo" [selected] < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor · Paseo 没开" [disabled] < combobox "junior-worker 的 host"
- option "grok · Paseo 没开" [selected] < combobox "junior-worker 的 host"
- option "claude · Paseo 没开" [disabled] < combobox "junior-worker 的 host"
- option "codex · Paseo 没开" [disabled] < combobox "junior-worker 的 host"
- option "pi · Paseo 没开" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" [disabled] < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model" [disabled]
- combobox "junior-worker 的 effort" [disabled] < dialog "本机配置"
- option "high" [selected] < combobox "junior-worker 的 effort" [disabled]
- text: Paseo 服务没开，问不到 grok 的 model；先开 Paseo，或者把 runner 换回 orca 或 herdr senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor · Paseo 没开" [disabled] < combobox "senior-worker 的 host"
- option "grok · Paseo 没开" [disabled] < combobox "senior-worker 的 host"
- option "claude · Paseo 没开" [disabled] < combobox "senior-worker 的 host"
- option "codex · Paseo 没开" [selected] < combobox "senior-worker 的 host"
- option "pi · Paseo 没开" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" [disabled] < dialog "本机配置"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model" [disabled]
- combobox "senior-worker 的 effort" [disabled] < dialog "本机配置"
- option "high" [selected] < combobox "senior-worker 的 effort" [disabled]
- text: Paseo 服务没开，问不到 codex 的 model；先开 Paseo，或者把 runner 换回 orca 或 herdr reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor · Paseo 没开" [disabled] < combobox "reviewer 的 host"
- option "grok · Paseo 没开" [disabled] < combobox "reviewer 的 host"
- option "claude · Paseo 没开" [selected] < combobox "reviewer 的 host"
- option "codex · Paseo 没开" [disabled] < combobox "reviewer 的 host"
- option "pi · Paseo 没开" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" [disabled] < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model" [disabled]
- combobox "reviewer 的 effort" [disabled] < dialog "本机配置"
- option "high" [selected] < combobox "reviewer 的 effort" [disabled]
- text: Paseo 服务没开，问不到 claude 的 model；先开 Paseo，或者把 runner 换回 orca 或 herdr verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor · Paseo 没开" [disabled] < combobox "verifier 的 host"
- option "grok · Paseo 没开" [disabled] < combobox "verifier 的 host"
- option "claude · Paseo 没开" [selected] < combobox "verifier 的 host"
- option "codex · Paseo 没开" [disabled] < combobox "verifier 的 host"
- option "pi · Paseo 没开" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" [disabled] < dialog "本机配置"
- option "sonnet 5" [selected] < combobox "verifier 的 model" [disabled]
- combobox "verifier 的 effort" [disabled] < dialog "本机配置"
- option "high" [selected] < combobox "verifier 的 effort" [disabled]
- text: Paseo 服务没开，问不到 claude 的 model；先开 Paseo，或者把 runner 换回 orca 或 herdr advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor · Paseo 没开" [disabled] < combobox "advisor 的 host"
- option "grok · Paseo 没开" [disabled] < combobox "advisor 的 host"
- option "claude · Paseo 没开" [selected] < combobox "advisor 的 host"
- option "codex · Paseo 没开" [disabled] < combobox "advisor 的 host"
- option "pi · Paseo 没开" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" [disabled] < dialog "本机配置"
- option "fable 5.1" [selected] < combobox "advisor 的 model" [disabled]
- combobox "advisor 的 effort" [disabled] < dialog "本机配置"
- option "medium" [selected] < combobox "advisor 的 effort" [disabled]
- text: Paseo 服务没开，问不到 claude 的 model；先开 Paseo，或者把 runner 换回 orca 或 herdr < dialog "本机配置"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 有 5 处 start 会拒绝，改好之前不能保存 带斜线的格子，下面一行写着哪里不对。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene changed
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" < combobox "senior-worker 的 host"
- option "codex" [selected] < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model"
- option "gpt 5.6 luna" < combobox "senior-worker 的 model"
- option "gpt 5.5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- option "ultra" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 没有改动 上面就是下一个新起的 agent 会用的配置。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene edited
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" [selected] < combobox "senior-worker 的 host"
- option "codex" < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "senior-worker 的 model"
- option "sonnet 5" < combobox "senior-worker 的 model"
- option "fable 5.1" < combobox "senior-worker 的 model"
- option "fable 5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 改了 1 处：senior-worker 的 host、model 保存后，下一个新起的 agent 就用新值；已经在跑的不受影响。 < dialog "本机配置"
- button "取消" < dialog "本机配置"
- button "保存" < dialog "本机配置"

## scene incomplete
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" [selected] < combobox "senior-worker 的 host"
- option "codex" < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "选一个 model" [disabled] [selected] < combobox "senior-worker 的 model"
- option "opus 5" < combobox "senior-worker 的 model"
- option "sonnet 5" < combobox "senior-worker 的 model"
- option "fable 5.1" < combobox "senior-worker 的 model"
- option "fable 5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" [disabled] < dialog "本机配置"
- option "—" [selected] < combobox "senior-worker 的 effort" [disabled]
- text: 还没选 model reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 有 1 处 start 会拒绝，改好之前不能保存 带斜线的格子，下面一行写着哪里不对。 < dialog "本机配置"
- button "取消" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene scanning
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 正在问每个 host 的 CLI 有哪些 model… cursor … grok … claude … codex … pi … runner 用什么起会话 < dialog "本机配置"
- combobox "runner" [disabled] < dialog "本机配置"
- option "herdr" < combobox "runner" [disabled]
- option "orca" [selected] < combobox "runner" [disabled]
- option "paseo" < combobox "runner" [disabled]
- option "按所在环境判断" < combobox "runner" [disabled]
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" [disabled] < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host" [disabled]
- option "grok" [selected] < combobox "junior-worker 的 host" [disabled]
- option "claude" < combobox "junior-worker 的 host" [disabled]
- option "codex" < combobox "junior-worker 的 host" [disabled]
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host" [disabled]
- combobox "junior-worker 的 model" [disabled] < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model" [disabled]
- option "grok 4.5" < combobox "junior-worker 的 model" [disabled]
- combobox "junior-worker 的 effort" [disabled] < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort" [disabled]
- option "medium" < combobox "junior-worker 的 effort" [disabled]
- option "high" [selected] < combobox "junior-worker 的 effort" [disabled]
- option "xhigh" < combobox "junior-worker 的 effort" [disabled]
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" [disabled] < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host" [disabled]
- option "grok" < combobox "senior-worker 的 host" [disabled]
- option "claude" < combobox "senior-worker 的 host" [disabled]
- option "codex" [selected] < combobox "senior-worker 的 host" [disabled]
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host" [disabled]
- combobox "senior-worker 的 model" [disabled] < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model" [disabled]
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model" [disabled]
- option "gpt 5.6 luna" < combobox "senior-worker 的 model" [disabled]
- option "gpt 5.5" < combobox "senior-worker 的 model" [disabled]
- combobox "senior-worker 的 effort" [disabled] < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort" [disabled]
- option "medium" < combobox "senior-worker 的 effort" [disabled]
- option "high" [selected] < combobox "senior-worker 的 effort" [disabled]
- option "xhigh" < combobox "senior-worker 的 effort" [disabled]
- option "max" < combobox "senior-worker 的 effort" [disabled]
- option "ultra" < combobox "senior-worker 的 effort" [disabled]
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" [disabled] < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host" [disabled]
- option "grok" < combobox "reviewer 的 host" [disabled]
- option "claude" [selected] < combobox "reviewer 的 host" [disabled]
- option "codex" < combobox "reviewer 的 host" [disabled]
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host" [disabled]
- combobox "reviewer 的 model" [disabled] < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model" [disabled]
- option "sonnet 5" < combobox "reviewer 的 model" [disabled]
- option "fable 5.1" < combobox "reviewer 的 model" [disabled]
- option "fable 5" < combobox "reviewer 的 model" [disabled]
- combobox "reviewer 的 effort" [disabled] < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort" [disabled]
- option "medium" < combobox "reviewer 的 effort" [disabled]
- option "high" [selected] < combobox "reviewer 的 effort" [disabled]
- option "xhigh" < combobox "reviewer 的 effort" [disabled]
- option "max" < combobox "reviewer 的 effort" [disabled]
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" [disabled] < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host" [disabled]
- option "grok" < combobox "verifier 的 host" [disabled]
- option "claude" [selected] < combobox "verifier 的 host" [disabled]
- option "codex" < combobox "verifier 的 host" [disabled]
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host" [disabled]
- combobox "verifier 的 model" [disabled] < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model" [disabled]
- option "sonnet 5" [selected] < combobox "verifier 的 model" [disabled]
- option "fable 5.1" < combobox "verifier 的 model" [disabled]
- option "fable 5" < combobox "verifier 的 model" [disabled]
- combobox "verifier 的 effort" [disabled] < dialog "本机配置"
- option "low" < combobox "verifier 的 effort" [disabled]
- option "medium" < combobox "verifier 的 effort" [disabled]
- option "high" [selected] < combobox "verifier 的 effort" [disabled]
- option "xhigh" < combobox "verifier 的 effort" [disabled]
- option "max" < combobox "verifier 的 effort" [disabled]
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" [disabled] < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host" [disabled]
- option "grok" < combobox "advisor 的 host" [disabled]
- option "claude" [selected] < combobox "advisor 的 host" [disabled]
- option "codex" < combobox "advisor 的 host" [disabled]
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host" [disabled]
- combobox "advisor 的 model" [disabled] < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model" [disabled]
- option "sonnet 5" < combobox "advisor 的 model" [disabled]
- option "fable 5.1" [selected] < combobox "advisor 的 model" [disabled]
- option "fable 5" < combobox "advisor 的 model" [disabled]
- combobox "advisor 的 effort" [disabled] < dialog "本机配置"
- option "low" < combobox "advisor 的 effort" [disabled]
- option "medium" [selected] < combobox "advisor 的 effort" [disabled]
- option "high" < combobox "advisor 的 effort" [disabled]
- option "xhigh" < combobox "advisor 的 effort" [disabled]
- option "max" < combobox "advisor 的 effort" [disabled]
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 正在扫描本机的 host 扫描完之前不能保存。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene saved
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" < combobox "senior-worker 的 host"
- option "codex" [selected] < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model"
- option "gpt 5.6 luna" < combobox "senior-worker 的 model"
- option "gpt 5.5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- option "ultra" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" [selected] < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 已保存 · 07:41 从下一个新起的 agent 开始用；已经在跑的不受影响。 < dialog "本机配置"
- button "关闭" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"

## scene refused
- dialog "本机配置"
- text: 本机配置 < dialog "本机配置"
- heading "这台机器上，每个角色跑在哪" [level=2] < dialog "本机配置"
- paragraph: 下拉菜单里的选项，是 MMW 刚问过这台机器上的 host 得到的，问的地方和 start 起会话时问的是同一处。这里就是 MMW 管这件事的唯一地方，保存在本机的 ~/.mmw/models.json；这一页不写 GitHub。 < dialog "本机配置"
- button: × < dialog "本机配置"
- paragraph: 没有保存。这一页打开之后，本机配置在 07:44 被别处改过（一个 agent 从命令行改的）。重新读取会换成现在保存着的内容，你刚才改的 1 处要再改一次。 < dialog "本机配置"
- button "重新读取" < dialog "本机配置"
- text: 本机的 host 07:02 问各 host 的 CLI · < dialog "本机配置"
- button "重新扫描" < dialog "本机配置"
- text: cursor 9 个 model grok 2 个 model claude 4 个 model codex 4 个 model pi orca、herdr 起不了 runner 用什么起会话 < dialog "本机配置"
- combobox "runner" < dialog "本机配置"
- option "herdr" < combobox "runner"
- option "orca" [selected] < combobox "runner"
- option "paseo" < combobox "runner"
- option "按所在环境判断" < combobox "runner"
- paragraph: 环境变量 MMW_RUNNER 设了时，它优先于这一格。「按所在环境判断」让 start 看自己跑在哪个 runner 里，判断不出时用 orca。runner 是 paseo 时，start 向 Paseo 要 model，所以换到 paseo 或从 paseo 换走，选项会重新扫描。 < dialog "本机配置"
- text: 一个角色一行 agent host model effort junior-worker 贴 junior-worker label 的票 < dialog "本机配置"
- combobox "junior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "junior-worker 的 host"
- option "grok" [selected] < combobox "junior-worker 的 host"
- option "claude" < combobox "junior-worker 的 host"
- option "codex" < combobox "junior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "junior-worker 的 host"
- combobox "junior-worker 的 model" < dialog "本机配置"
- option "grok 4.6" [selected] < combobox "junior-worker 的 model"
- option "grok 4.5" < combobox "junior-worker 的 model"
- combobox "junior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "junior-worker 的 effort"
- option "medium" < combobox "junior-worker 的 effort"
- option "high" [selected] < combobox "junior-worker 的 effort"
- option "xhigh" < combobox "junior-worker 的 effort"
- text: senior-worker 贴 senior-worker label 的票 < dialog "本机配置"
- combobox "senior-worker 的 host" < dialog "本机配置"
- option "cursor" < combobox "senior-worker 的 host"
- option "grok" < combobox "senior-worker 的 host"
- option "claude" < combobox "senior-worker 的 host"
- option "codex" [selected] < combobox "senior-worker 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "senior-worker 的 host"
- combobox "senior-worker 的 model" < dialog "本机配置"
- option "gpt 6 astra" < combobox "senior-worker 的 model"
- option "gpt 5.6 sol" [selected] < combobox "senior-worker 的 model"
- option "gpt 5.6 luna" < combobox "senior-worker 的 model"
- option "gpt 5.5" < combobox "senior-worker 的 model"
- combobox "senior-worker 的 effort" < dialog "本机配置"
- option "low" < combobox "senior-worker 的 effort"
- option "medium" < combobox "senior-worker 的 effort"
- option "high" [selected] < combobox "senior-worker 的 effort"
- option "xhigh" < combobox "senior-worker 的 effort"
- option "max" < combobox "senior-worker 的 effort"
- option "ultra" < combobox "senior-worker 的 effort"
- text: reviewer 评审每张票的改动 < dialog "本机配置"
- combobox "reviewer 的 host" < dialog "本机配置"
- option "cursor" < combobox "reviewer 的 host"
- option "grok" < combobox "reviewer 的 host"
- option "claude" [selected] < combobox "reviewer 的 host"
- option "codex" < combobox "reviewer 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "reviewer 的 host"
- combobox "reviewer 的 model" < dialog "本机配置"
- option "opus 5" [selected] < combobox "reviewer 的 model"
- option "sonnet 5" < combobox "reviewer 的 model"
- option "fable 5.1" < combobox "reviewer 的 model"
- option "fable 5" < combobox "reviewer 的 model"
- combobox "reviewer 的 effort" < dialog "本机配置"
- option "low" < combobox "reviewer 的 effort"
- option "medium" < combobox "reviewer 的 effort"
- option "high" [selected] < combobox "reviewer 的 effort"
- option "xhigh" < combobox "reviewer 的 effort"
- option "max" < combobox "reviewer 的 effort"
- text: verifier 复验每张票的判据 < dialog "本机配置"
- combobox "verifier 的 host" < dialog "本机配置"
- option "cursor" < combobox "verifier 的 host"
- option "grok" < combobox "verifier 的 host"
- option "claude" [selected] < combobox "verifier 的 host"
- option "codex" < combobox "verifier 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "verifier 的 host"
- combobox "verifier 的 model" < dialog "本机配置"
- option "opus 5" < combobox "verifier 的 model"
- option "sonnet 5" [selected] < combobox "verifier 的 model"
- option "fable 5.1" < combobox "verifier 的 model"
- option "fable 5" < combobox "verifier 的 model"
- combobox "verifier 的 effort" < dialog "本机配置"
- option "low" < combobox "verifier 的 effort"
- option "medium" < combobox "verifier 的 effort"
- option "high" < combobox "verifier 的 effort"
- option "xhigh" < combobox "verifier 的 effort"
- option "max" [selected] < combobox "verifier 的 effort"
- text: advisor 被问到时给第二意见 < dialog "本机配置"
- combobox "advisor 的 host" < dialog "本机配置"
- option "cursor" < combobox "advisor 的 host"
- option "grok" < combobox "advisor 的 host"
- option "claude" [selected] < combobox "advisor 的 host"
- option "codex" < combobox "advisor 的 host"
- option "pi · orca、herdr 起不了" [disabled] < combobox "advisor 的 host"
- combobox "advisor 的 model" < dialog "本机配置"
- option "opus 5" < combobox "advisor 的 model"
- option "sonnet 5" < combobox "advisor 的 model"
- option "fable 5.1" [selected] < combobox "advisor 的 model"
- option "fable 5" < combobox "advisor 的 model"
- combobox "advisor 的 effort" < dialog "本机配置"
- option "low" < combobox "advisor 的 effort"
- option "medium" [selected] < combobox "advisor 的 effort"
- option "high" < combobox "advisor 的 effort"
- option "xhigh" < combobox "advisor 的 effort"
- option "max" < combobox "advisor 的 effort"
- paragraph: 一台新机器第一次安装时，这里填的是 MMW 自带的初始值；之后只按这里选的跑，MMW 更新不会改它。 < dialog "本机配置"
- text: 没有保存 这一页打开之后，本机配置被别处改过，先重新读取。 < dialog "本机配置"
- button "取消" < dialog "本机配置"
- button "保存" [disabled] < dialog "本机配置"
