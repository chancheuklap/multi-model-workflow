{
  "mcpServers": {
    "serena": {
      "command": "bash",
      "args": [
        "-c",
        "set -euo pipefail; SCRIPT=\"$HOME/.cursor/plugins/local/multi-model-workflow-cursor/scripts/serena-mcp.sh\"; if [ ! -x \"$SCRIPT\" ]; then for d in \"$HOME\"/.cursor/plugins/*/*/multi-model-workflow-cursor \"$HOME\"/.cursor/plugins/*/multi-model-workflow-cursor; do [ -x \"$d/scripts/serena-mcp.sh\" ] || continue; SCRIPT=\"$d/scripts/serena-mcp.sh\"; break; done; fi; [ -x \"$SCRIPT\" ] || { echo \"ERROR: serena-mcp.sh not found under $HOME/.cursor/plugins\" >&2; exit 2; }; exec \"$SCRIPT\""
      ],
      "env": {
        "SERENA_PROJECT": "${workspaceFolder}",
        "MMW_SERENA_LAUNCHER": "v3-bash-home"
      }
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "env": {
        "CONTEXT7_API_KEY": "${env:CONTEXT7_API_KEY}"
      },
      "envFile": "${userHome}/.cursor/context7.env"
    }
  }
}
