# plugin marketplace override

- This marketplace exists only to expose the repo-local Codex plugin under `codex-orchestrate-new/`.
- Do not add unrelated plugins or external marketplace entries here while maintaining the Codex plugin migration.
- Marketplace changes must stay aligned with `codex-orchestrate-new/.codex-plugin/plugin.json`.
