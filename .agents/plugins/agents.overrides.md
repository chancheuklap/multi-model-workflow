# plugin marketplace override

- This marketplace exists only to expose the repo-local Codex replica under `codex-orchestrate/`.
- Do not add unrelated plugins or external marketplace entries here while porting the Claude plugin.
- Marketplace changes must stay aligned with `codex-orchestrate/.codex-plugin/plugin.json`.
