# plugins marketplace overrides

- This folder exposes the repo-local Codex plugin marketplace for `codex-orchestrate`.
- Keep marketplace paths relative to the repository root so `codex plugin marketplace add <repo-root>` can install the package.
- Do not add old Codex Orchestrate runtime entries here.
