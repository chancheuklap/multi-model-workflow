.DS_Store
*.swp
*.swo
*~
.claude/*
!.claude/skills/
.worktrees/
.codex/multi-model-workflow/
.serena/
graphify-out/
node_modules/
.reviews/
.scratch/
__pycache__/
.dispatch/
.release/
.playwright-cli/

# implement 技能开工时写在工人 worktree 根的关卡状态文件；Stop hook 读它决定放不放行。每个 worktree 一份，不入库。
.mmw-ticket-state.json

# 角色物化产物。安装时从源码渲染进各宿主目录，仓库不留副本。
# Pi 那一份落在 runtime 的包目录里（它的包合同要求 agent 文件在包内）。
mmw/agents-pi/
mmw/codex/agents/

# 语言工具（检查器与语言服务器）。由 mmw-v2/tools/install.sh 装，每次装最新稳定版，
# 所以不入库也不锁版本。删掉重跑一次安装器就全回来。
mmw-v2/tools/bin/
mmw-v2/tools/uv/
mmw-v2/tools/node/

# 界面 QA 的运行时依赖，由技能自带的 install-deps.sh 按 deps.json 装。
# 版本钉死在声明里，装出来的东西不进 git。
mmw-v2/skills/ui-qa/scripts/deps/

# 纪律注入层的完成拦截（mmw-v2/hooks/mmw-stop.mjs）在被拦的 worktree 根留下的本地状态：
# 拦截计数与文件锁。任何装了 hook 层的宿主在任何仓库里都会写，所以本仓也要忽略。
.mmw/
.mmw-hook-state.json
