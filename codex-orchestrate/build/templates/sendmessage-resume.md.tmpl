**Codex subagent resume template**（修复路径必须优先恢复原 agent，禁止无记录地新建 agent）：

[variant=worker]
**Worker resume 步骤**（pack_executor / complex_pack_executor 修复）：

1. `state.sh agent-id get --run-id <run_id> --pack-id <pack_id>` 读取 execution-state 中的 agent_id
2. 调用：
   ```
   send_input({
     target: "<agent_id>",
     message: "<含 DISPATCH_ENVELOPE 的修复 prompt，repair_round >= 1>"
   })
   ```
3. 如果 Codex host 返回 agent 已关闭，调用 `resume_agent({ id: "<agent_id>" })` 后再次 `send_input({ target: "<agent_id>", message: "<repair prompt>" })`。
4. 如果原 worker resume 仍失败，Coordinator 必须记录 exception_code，带完整原 Pack Brief、accepted finding ids、prior attempt summary 新建 replacement dispatch；不得静默重派。
5. 等待原 worker 返回（可用 `wait_agent({ targets: ["<agent_id>"] })`）
6. 解析返回结果 -> `state.sh transition --actor Coordinator --to returned`
7. 修复完成后运行 verification commands + 对照 acceptance criteria + grep 确认变更
8. `state.sh self-verify append --run-id <run_id> --pack-id <pack_id> --repair-round <N> --verification-passed <yes|no> --exception <none|3plus_files_control_flow|user_requested|rca_root_cause|path_a_self_fix|host_resume_failed>`
9. 写 `state.sh disposition append` 或 `state.sh update --field plans[N].packs[M].repair_round`
[/variant]

[variant=plan_writer]
**Plan-Writer resume 步骤**（plan_writer 修复）：

1. `state.sh read --run-id <run_id> --field '.plan_writer_agent_id'` 读取 workflow-state 中的 plan_writer_agent_id
2. 若返回 null/empty -> 立即标记 BLOCKED 给用户 + `state.sh transition --actor Coordinator --to blocked`（不允许创建新 agent）
3. 调用：
   ```
   send_input({
     target: "<plan_writer_agent_id>",
     message: "<含 DISPATCH_ENVELOPE 的修复 prompt，repair_round >= 1>"
   })
   ```
4. 如果 Codex host 返回 agent 已关闭，调用 `resume_agent({ id: "<plan_writer_agent_id>" })` 后再次 `send_input({ target: "<plan_writer_agent_id>", message: "<repair prompt>" })`。
5. 如果 host resume 仍失败，Coordinator 必须记录 exception_code，带完整 reviewed design、issue hierarchy、current plan path、accepted findings 新建 replacement dispatch；不得静默重派。
6. 等待 agent 返回（同步或 wait_agent）
7. 解析返回结果 -> `state.sh transition --actor Coordinator --to returned`
8. 验证 plan 文件格式 + pack count validator
9. `state.sh self-verify append --run-id <run_id> --repair-round <N> --verification-passed <yes|no>`
10. 回到 Plan Review 重审
[/variant]
