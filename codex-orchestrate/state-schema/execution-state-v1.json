{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "execution-state-v1",
  "description": "Pack-level execution state for multi-model-workflow orchestration. Per Ruling 2: pack-level data (status, agent_id, worker_thread_id, worker_job_file, commit_sha, worker_verdict) lives here, not in workflow-state.",
  "type": "object",
  "required": ["run_id", "plans"],
  "properties": {
    "run_id": { "type": "string" },
    "plans": {
      "type": "object",
      "description": "Keyed by plan ID (e.g., '001', '002')",
      "additionalProperties": {
        "type": "object",
        "required": ["packs"],
        "properties": {
          "packs": {
            "type": "object",
            "description": "Keyed by pack ID (e.g., '1.1', '1.2')",
            "additionalProperties": {
              "type": "object",
              "required": ["status"],
              "properties": {
                "status": {
                  "type": "string",
                  "enum": ["pending", "dispatched", "returned", "committed", "blocked"]
                },
                "agent_id": {
                  "type": ["string", "null"],
                  "description": "Interactive Codex subagent id, or codex-exec:<thread_id> for worktree-exec workers."
                },
                "worker_backend": {
                  "type": ["string", "null"],
                  "enum": ["codex-subagent", "codex-exec", null]
                },
                "worker_thread_id": { "type": ["string", "null"] },
                "worker_job_file": { "type": ["string", "null"] },
                "commit_sha": { "type": ["string", "null"] },
                "worker_verdict": { "type": ["string", "null"] },
                "repair_round": { "type": "integer", "default": 0 }
              }
            }
          }
        }
      }
    }
  }
}
