One related issue. A blocker still holding reads "held"; a row the tree cannot read does not link.

```jsx
<RelationRow ticket row={r} onGoto={go} data-ui="详情.blocker" />
```

It renders the product's own markup and classes; pass the product's view data as is.
