Where a ticket runs, or the line saying it has not run.

```jsx
<RunBox runtime={view.runtime} noRunText="not dispatched yet" data-ui="详情.runtime" />
```

It renders the product's own markup and classes; pass the product's view data as is.
