The first row of the detail column.

```jsx
<DetailHead ticket eyebrow="Ticket" onGithub={gh} onClose={close} data-ui="详情.head" />
```

It renders the product's own markup and classes; pass the product's view data as is.
