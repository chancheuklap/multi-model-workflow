Open the shown map, spec or decision ticket on GitHub; last in the column.

```jsx
<GithubButton label="在 GitHub 打开 #131 ↗" onClick={gh} />
```

It renders the product's own markup and classes; pass the product's view data as is.
