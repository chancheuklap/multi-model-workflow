When the hosts were last asked, and a rescan; a spinner while asking.

```jsx
<ScanStatus scannedText="07:02 问各 host 的 CLI ·" onRescan={rescan} />
```

It renders the product's own markup and classes; pass the product's view data as is.
