Which runner starts sessions.

```jsx
<RunnerRow cls={v.runnerCls} value={v.runner} opts={v.runnerOpts} onChange={set} />
```

It renders the product's own markup and classes; pass the product's view data as is.
