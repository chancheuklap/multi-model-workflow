A dropdown of the sheet; its class says changed or refused.

```jsx
<Select cls="sel" value="orca" label="runner" opts={opts} onChange={set} />
```

It renders the product's own markup and classes; pass the product's view data as is.
