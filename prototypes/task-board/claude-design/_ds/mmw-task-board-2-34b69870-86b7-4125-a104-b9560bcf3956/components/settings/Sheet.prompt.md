The settings sheet over a dimmed page; its body is SetBlocks, its foot says what a save would change.

```jsx
<Sheet store="~/.mmw/models.json" strong="没有改动" quiet="…" closeLabel="关闭" saveOff>…</Sheet>
```

It renders the product's own markup and classes; pass the product's view data as is.
