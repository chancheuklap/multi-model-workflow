When the page last read GitHub, or, hatched, that the last read failed and which data is shown.

```jsx
<ReadState text="只读 · 07:39 读取" />
```

It renders the product's own markup and classes; pass the product's view data as is.
