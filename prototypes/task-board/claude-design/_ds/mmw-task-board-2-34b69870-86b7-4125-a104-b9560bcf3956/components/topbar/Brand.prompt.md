The top bar's word mark and repository name. Use it once, at the left of the top bar.

```jsx
<Brand repo="owner/name" data-ui="顶栏.brand" />
```

It renders the product's own markup and classes; pass the product's view data as is.
