A square icon button at the right end of the top bar: read GitHub now, or open the settings sheet (`on` while it is open).

```jsx
<IconButton icon="settings" label="本机配置" onClick={open} />
```

It renders the product's own markup and classes; pass the product's view data as is.
