A ticket on the canvas: lamp, number, step, title and where it runs.

```jsx
<TicketCard item={t} onPick={pick} data-ui="画布.ticket-card" />
```

It renders the product's own markup and classes; pass the product's view data as is.
