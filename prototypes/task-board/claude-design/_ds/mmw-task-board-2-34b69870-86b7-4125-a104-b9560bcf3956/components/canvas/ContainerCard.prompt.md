A map or spec on the canvas trunk, with its landed count, expand chevron and progress bar.

```jsx
<ContainerCard item={c} onPick={pick} onToggle={toggle} data-ui="画布.container-card" />
```

It renders the product's own markup and classes; pass the product's view data as is.
