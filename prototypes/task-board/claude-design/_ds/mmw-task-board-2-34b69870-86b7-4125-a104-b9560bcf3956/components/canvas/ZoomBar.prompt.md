Zoom out, the level, zoom in and fit, bottom right of the canvas.

```jsx
<ZoomBar level="90%" onOut={out} onIn={zin} onFit={fit} data-ui="画布.zoom" />
```

It renders the product's own markup and classes; pass the product's view data as is.
