The uppercase heading over the task list with its count.

```jsx
<ColumnEyebrow label="The Night" count={3} />
```

It renders the product's own markup and classes; pass the product's view data as is.
