/** What the task list says when no ticket carries the map label. */
export interface TasksEmptyProps {
  /** Its id, set on the root; each part carries `<data-ui>.<part>`. */
  "data-ui"?: string;
}
export declare function TasksEmpty(props: TasksEmptyProps): JSX.Element;
