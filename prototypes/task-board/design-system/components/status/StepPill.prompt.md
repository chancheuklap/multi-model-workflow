The step a ticket is at, named in a coloured capsule. Use it on ticket cards, relation rows, event blocks and step counts.

```jsx
<StepPill phase="working" />
<StepPill phase="verify" big />
```

It renders the product's own markup and classes; pass the product's view data as is.
