One event: time, name, one line, and, opened, every payload field.

```jsx
<EventRow item={e} opened={false} onToggle={t} data-ui="详情.event" />
```

It renders the product's own markup and classes; pass the product's view data as is.
