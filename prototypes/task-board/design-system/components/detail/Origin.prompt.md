The shown issue's number and links to its spec and map.

```jsx
<Origin ticket num="#133" links={[{label: "spec #131", n: 131}]} onGoto={go} />
```

It renders the product's own markup and classes; pass the product's view data as is.
