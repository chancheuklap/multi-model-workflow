A ruled block of the detail column with its title and count.

```jsx
<Section ticket title="Blocked by" note={1}>…</Section>
```

It renders the product's own markup and classes; pass the product's view data as is.
