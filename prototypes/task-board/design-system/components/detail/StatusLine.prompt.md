The lamp, its word, the step (tickets) and the elapsed time or landed count.

```jsx
<StatusLine ticket lamp="orange" statusWord="needs you" phase="working" elapsed="1h02m" />
```

It renders the product's own markup and classes; pass the product's view data as is.
