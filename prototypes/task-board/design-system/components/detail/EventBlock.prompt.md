One step of a ticket's history; open, it holds an EventRow per event.

```jsx
<EventBlock block={b} opened onToggle={t}>{rows}</EventBlock>
```

It renders the product's own markup and classes; pass the product's view data as is.
