Why the lamp is orange, one line per reason. Show it only when there is one.

```jsx
<NeedsYou why={view.why} data-ui="详情.why" />
```

It renders the product's own markup and classes; pass the product's view data as is.
