A decision ticket of a map, lower than a ticket, with its kind instead of a step.

```jsx
<DecisionCard item={d} onPick={pick} data-ui="画布.decision-card" />
```

It renders the product's own markup and classes; pass the product's view data as is.
