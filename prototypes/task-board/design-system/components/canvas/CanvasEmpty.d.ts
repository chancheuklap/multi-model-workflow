/** What the canvas says before any task exists. */
export interface CanvasEmptyProps {
  /** Its id, set on the root; each part carries `<data-ui>.<part>`. */
  "data-ui"?: string;
}
export declare function CanvasEmpty(props: CanvasEmptyProps): JSX.Element;
