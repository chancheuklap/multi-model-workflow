A caption over a band of cards, or the warning over a blocking cycle.

```jsx
<LaneLabel label={l} data-ui="画布.lane-label" />
```

It renders the product's own markup and classes; pass the product's view data as is.
