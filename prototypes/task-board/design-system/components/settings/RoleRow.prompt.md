One agent's host, model and effort dropdowns and their problems. Pass a row of the product's settingsView.

```jsx
<RoleRow row={r} onCell={set} data-ui="本机配置.role" />
```

It renders the product's own markup and classes; pass the product's view data as is.
