A save refused because the configuration changed elsewhere.

```jsx
<RefusedBanner text="…" onReread={reread} />
```

It renders the product's own markup and classes; pass the product's view data as is.
