[project]
name = "fixture-app"
version = "0.1.0"
description = "Test bench package for the code-landing fixture ticket"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
