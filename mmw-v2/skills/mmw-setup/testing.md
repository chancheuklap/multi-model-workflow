# TESTING.md（本仓库薄层）

> 测试怎么写、够不够格进仓库，是插件随身带的：`/mmw-tdd` 那个技能，加它旁边的 `tests.md`、`mocking.md`、`quality-bar.md`。本文只补**本仓库的事实**，不重复也不删减「测试怎么写」和「够不够格进仓库」这两层。这里写的任何内容都不能豁免 `quality-bar.md` 的资格线，也不能放行它列的禁止形态。

## 目录分层

写清楚这个仓库的测试分几层、各自放在哪、各自测什么。一行一层。

| 层 | 位置 | 测什么 |
| --- | --- | --- |
| <例如：单元> | <例如：`tests/unit/`> | <这一层守的是哪一类行为> |
| <例如：集成> | <例如：`tests/integration/`> | <同上> |

## 外部接缝

`mocking.md` 说只在系统边界上 mock。**哪些依赖算这个仓库的外部边界，在这里列清楚**——列出来的允许打桩，没列出来的一律走真代码。

- <例如：支付服务商的 HTTP 接口——用录制下来的响应>
- <例如：系统时间——测试里定死>

## 权威源指针

`quality-bar.md` 要求价格、文案、枚举这类值从源头读出来比对，不重打一遍。**这个仓库的源头在哪，在这里指明**。

| 值 | 权威源 |
| --- | --- |
| <例如：套餐价格> | <例如：`config/pricing.yaml`> |

## 怎么跑

整套测试的命令，加上提交之前必须过的那些检查。

```bash
<例如：uv run pytest>
```

## 这个仓库特有的边界

前面四节装不下、但写测试的人必须知道的约定写在这里。没有就删掉本节。
