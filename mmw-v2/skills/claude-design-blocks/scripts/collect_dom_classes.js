// Run on the all-scenario harness page via playwright-cli --raw eval; returns the object {component: {data-dc-tpl index: {class: 1}}}. playwright-cli --raw eval serializes it once; save the output as domclasses.json for flatten.py.
// The harness page (mkallharness.py) wraps each instance as <div data-blk="<component>"><dc-import name="<component>" scenario="…"></dc-import></div>
(() => { const out = {}; document.querySelectorAll('[data-blk]').forEach(d => { const b = d.dataset.blk; out[b] = out[b] || {}; d.querySelectorAll('[data-dc-tpl]').forEach(e => { const id = e.getAttribute('data-dc-tpl'); const cls = (typeof e.className === 'string' ? e.className : (e.className.baseVal || '')).split(/\s+/).filter(Boolean); const set = out[b][id] = out[b][id] || {}; cls.forEach(c => set[c] = 1); }); }); return out; })()
