---
name: mmw-dispatching-agents
description: 把一件任务派给隔离上下文的劳动力——Claude 会话内 sub-agent，或 Codex 无头进程。管选后端、钉模型档、围栏、简报自包含、怎么收回。需要把任务派出去时用它。
---

# 派劳动力

隔离上下文是目的：审者不该看见写者的推理过程，几路视角之间也不该互相污染。派出去还顺带拿到并行。

模型和档位一律从目标仓库的 `docs/agents/models.md` 取。**本技能正文和任何提示词里都不出现型号**——换型号只改那份文件。

## 两个后端

| 后端 | 怎么起 | 什么时候用 |
| --- | --- | --- |
| Claude | `Agent` 工具，`subagent_type: general-purpose` | 派 Claude 那一家 |
| Codex | `codex exec` 无头进程，Bash 后台起 | 派 Codex 那一家 |

派哪一家由 `models.md` 的红线定：每一道审至少有一路审者与作者不同家。

### Codex 无头怎么起

```bash
codex exec -C . --sandbox read-only --color never \
  -m <模型> -c model_reasoning_effort="<档位>" \
  -o <回执文件> \
  - < <提示词文件>
```

- 用 Bash 的 `run_in_background: true` 起。审一轮、写一份计划，都常常超过前台超时上限。
- `-o` 把它最后一条消息写进回执文件。读那个文件就是回执，不用从事件流里捞。
- 提示词走 stdin，所以先用 Write 把它写成文件。
- 派可写任务时换 `--sandbox workspace-write`。**首次派发前工作区必须干净**，否则验收读 diff 时分不清哪些改动是它的。
- **要它自己提交，还得单独放开 `.git`。** `workspace-write` 默认把 `.git` 锁成只读，工人写完代码会卡在 `.git/index.lock: Operation not permitted`。加这个覆盖：

  ```bash
  -c 'sandbox_workspace_write.writable_roots=["<worktree 绝对路径>/.git"]'
  ```

  放开之后它理论上也能改历史，所以派它的时候要说清：只许 `add` 加 `commit`，不许 `amend`、`rebase`、`reset` 或强推。
- 续接用 `codex exec ... resume <会话号> "<追问>"`。**resume 不继承原来的围栏和模型档，整套重钉。** 会话号要用就加 `--json` 起，在事件流的 `thread_id` 里。
- **不用 `codex review`。** 它自带提示词，会绕过我们的审查方法。

## 简报自包含

无头那一侧看不见我们的插件文件。给它一个插件内路径，它读不到，然后自己编一个。

所以**方法论原文粘进提示词，不给插件内路径**。被审仓库里的路径可以给——它 `-C .` 就在那个仓库里，读得到。

同一路活派给两家时用**同一份简报文本**，只有「你负责哪一路」那一句不同。两家看的是同一套方法，交回的 findings 才对得上。

派发前自检：简报里提到的每个仓库内路径真实存在。缺了当场报错，不让它开工之后才发现。

## 并行

一条消息里发多个 `Agent` 调用，它们并行跑。Codex 那几个各自后台起，也并行。两边可以同时在跑。

## 下一步

| 情况 | 下一步 |
| --- | --- |
| 派出去的都交回了回执 | **移交**：`/mmw-judging-agent-output`。回执是证据不是结论，逐条复核过才能用，绝不原样转发 |
| 回执里说它自己停下了 | **自己继续**：读它的尝试记录，把你看到的发回去，用 `resume` 续接同一个会话。**resume 不继承原来的围栏和模型档，整套重钉** |
| 派发前自检发现简报里有不存在的仓库内路径 | **自己继续**：当场修掉路径再派，不要让它开工之后才发现 |
| 要派可写任务，但工作区不干净 | **停**：报工作区里有哪些未提交的改动。工作区不干净，验收读 diff 时分不清哪些改动是它的 |
