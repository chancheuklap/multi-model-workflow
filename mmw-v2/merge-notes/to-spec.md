# to-spec

源目录：`mmw-v2/upstream/skills/engineering/to-spec/`

## 逐段意图

### SKILL.md

| 段落 | 我们的意图 |
| --- | --- |
| frontmatter 的 `description` | 前半句是上游的，我们在末尾补一句触发：``Use when a conversation, a wayfinder map or a triaged issue has to become a spec, or a section of a published spec has to change.``。这份技能在本仓是模型可触发的（下一行），而 host 启动时只扫这一行；四支触发各对应正文一个入口：对话、第 1 步的 wayfinder `map`、`triage` 技能第 5 步送来的外来 issue、第 5 步的修订。**引号也不能去**。值里 `tracker: no` 是一个冒号加空格，YAML 会把不带引号的这一行读成一个嵌套 mapping 的 key，`yaml.safe_load` 对整块 frontmatter 抛 `ScannerError: mapping values are not allowed here`——坏的不是这一个 key，是整份 frontmatter。而 `description` 恰好是每个 host 启动时唯一扫进技能列表的那一行，所以症状不是一声响的失败，是这份技能整个安静地消失；再加上改这一行要重开会话才生效，改动和症状之间还隔着一段距离。理由写在这里，是因为 `description` 的值里装不下注释。别人要求「把 description 统一成不带引号」时 → 这一份是例外，值里含冒号加空格的都是。上游改前半句 → 收上游措辞，末尾这一句保留 |
| frontmatter 的 `disable-model-invocation: true` | 删掉。本仓要求这个 skill 模型可触发——不留上游的人工触发限制，免得漏输入指令时 agent 没法自己认出该用它。上游改这一行 → 仍然删，跟 `agents/openai.yaml` 的 `policy` 块同增同删 |
| 第 3 步的 seam 由 agent 定、不问 user 那一段，与 `<spec-template>` 里 `## Testing Decisions` 的 `How a test arrives at a state` 那一项 | 我们加的整块。seam 只回答测试在哪**观察**；测试怎么**到达**要测的状态，上游没有问处。判据是「声明的 seam 的写入面够不着」：真数据库的 seam 里测试直接改行就到了，不用写；有 screen contract 时，story 页由 adapter 读该 scene 的值进入场景，测不到只有真实请求才产生的产品状态，four-column boundary test 断言 `calls` / `shows` / `next` / `on_failure`、仍写不进这次点击本应写入的数据库，这两种够不着就必须在这里点名将来靠什么到达、以及那条通路存在于哪些构建。两例加「With a screen contract」门，用登记名 **four-column boundary test**。形态按 consuming repository 的 `TESTING.md` 给，它没出口时是那个 repository 要补、不是 spec 去裁定——MMW v2 不该知道某个 repository 的测试规矩写了第几条；spec 只「say so」，后面接一句由 `to-tickets` 出 `reach` ticket，这样这个状态有终点，不在两份技能之间来回。第 3 步明写 seam 由 agent 自己定、不让 user 确认——user 看不懂 seam，问他等于把自己该做的判断推给读不懂的人，也跟开头「只交还一个判断」自相矛盾；user 看到的只是 `## Testing Decisions` 首句那句大白话。上游自己给 `## Testing Decisions` 加了要求 → 收上游措辞，这一项保留；上游加回「问 user 确认 seam」→ 不收；上游把够不着的例子改回 debugging port 或去掉 screen-contract 门 → 不收，story 页与 four-column boundary test 两例保留 |
| 第 3 步末句「The repository's `TESTING.md` says which test layers exist …」 | 我们加的。仓库的规则分两份文件：`CODING_STANDARDS.md` 管产品代码，`TESTING.md` 管测试（分层、哪些外部边界可以打桩、怎么跑）。seam 与 `## Testing Decisions` 从 `TESTING.md` 取，写进 spec；worker 不读这份文件。用户的决定。上游改第 3 步 → 收上游措辞，这一句接在末尾 |
| 第 4 步 publish 那一句的 label 规则 | 我们改的：spec 只打一个 label，分类 label `mmw:spec`（仓库没有就按 `docs/agents/issue-tracker.md` `## Three label sets` 建，颜色与说明只写在那一处），不打任何队列 label。spec 是它底下那批 ticket 的容器，不是一件待办。不打 `ready-for-agent` 的 spec 三道关全都过不去：进不了 `is:open label:ready-for-agent` 这条 agent queue，过不了 `preflight` 的第四项（`NOT_READY: … has no ready-for-agent label`），也过不了 `dispatch.sh` 派活前的查票（`REFUSE ticket #… is not labelled ready-for-agent`）。分类 label 不进任何队列，它回答的是「这张票是哪一层」，board 靠它把 spec 和 ticket 分开，不必数嵌套深度——所以原来那句「Leave it unlabelled」的理由只反对队列 label，不反对分类 label（mmw #315 第 6 节，用户确认）。正文只留「不打队列 label」的理由，分类 label 为什么不进队列、board 靠它分层，只写在这里。三套 label 的分工写在 `docs/agents/issue-tracker.md` 的 **Three label sets**。上游改这句措辞 → 仍然不打队列 label，`mmw:spec` 保留 |
| 第 4 步 publish 的 native parent 两段 | 我们加的整块，接在 publish 那一句之后，是 map child spec 的 prompt contract，不得缩成「记得设 parent」。引用是 wayfinder map 时：每份 spec 都用 `gh issue create --parent <map>` 建成 map 的 native sub-issue，读回 `parent.number` 必须等于 map 号，缺了或不同就是 failed publish，交给下一手之前改掉；`## Sources`、`## Further Notes`、spec title、semantic similarity 都不能替代 native parent。引用不是 wayfinder map 时：do not invent a map parent，conversation / file / standalone issue / 其它非 map 来源没有 native parent，除非来源自己已经带一个。不写 GitHub 以外的 tracker：landing pipeline 只跑在 GitHub 上（每个 consuming repository 都在 GitHub，`setup-matt-pocock-skills` 按 `git remote` 提议 tracker），为别的 tracker 写的句子没有 run 走得到。第 1 步的 one-spec/several-spec 判断和 `## Specs` 写回顺序不动；后一份从同一 map 发布的 spec 走同一段，不只第一份。上游改第 4 步 → 收上游措辞，这两段仍接在 publish 之后 |
| `## Process` 的第 1 步，与 `references/several-specs.md` | 我们加的整步，上游原来的三步在它后面顺延：user 传了引用就先读它的正文与评论（外来 issue 的 agent brief 是一条评论），是 wayfinder `map` 时按 `Decisions so far` 逐张读 resolution comment、读到 prototype 与 research file 的结论、`Out of scope` 原样进 spec；然后判一份还是几份 spec（同一个 seam 归一份，能不分就不分；分阶段交付里后一份依赖前一份是允许的，只要依赖单向不成环、顺序写进 `## Specs`——上游那条「实现票不依赖别的部分」的判据下没有任何分阶段交付的产品能切成多份，而 `--lint` 把 cross-batch 的 blocker 记 `WARN`，正是为它留的口子）。一份就写；几份、或引用已经带着划分时，读 `references/several-specs.md`：几份时问 user 确认并把划分写回 `map` 的 `## Specs`，只写第一份，publish 后回填链接再停。引用不是 map（issue、URL、文件、对话）时没有 map 可写回，划分写进第一份 spec 的 `## Further Notes`（每份一行：叫什么、覆盖什么、顺序、发布后的链接），停下时告诉 user 下次拿这份 spec 的 issue 号跑本技能——引用是对话时，下一个 session 没有那段对话，只有这个号能把它带回划分；引用是一份 `## Further Notes` 带划分的 spec 时，写其中第一份还没有链接的、经 `references/revising-a-spec.md` 回填链接，全部有链接时直接告诉 user 已写完——`<spec-template>` 的 `## Further Notes` 说明里也写了这个形状。划分这一支挪进 reference，是因为多数 run 只写一份 spec，不必每次读这两段。上游改了 `## Process` 的编号或在前面插步 → 收上游的顺序，我们这一步永远排第一（它决定这次到底写几份 spec）|
| `## Process` 的第 5 步「Revising a published spec」、`references/revising-a-spec.md`，与第 1 步里指向它的那一句 | 我们加的：引用是已发布的 spec issue、其中一节要改时（user 要改，或 `to-tickets` 第 4 步把 agent 退回来），第 1 步直接去读 `references/revising-a-spec.md`；第 5 步只剩一句指向同一份文件，留着是为了按步名（`the to-spec skill's step for revising a published spec`，不写步号）指过来的调用方还找得到它。那份文件：读全正文、直接干净地改那一节、`gh issue edit --body-file` 写回，正文不留改动痕迹（正文用正面说法 `reads as if written that way from the start`，不再逐项列删除线、updated、日期注这些禁项），改了什么、为什么写成一条评论，已出的 ticket 对着新文本核一遍；前提消失的 criterion 拿出它那张票的 `## Acceptance criteria`、编号不复用、收尾评论说明去向（从 `to-tickets` 第 4 步挪来：拿掉它的是修订的这一方）；`## When the rows change`（从 `to-tickets` 的 `references/cutting-interface-tickets.md` 挪来：重新 pull 之后合同行变了，已切的票怎么改）。挪成 reference 的理由：修订是别的技能在另一个时刻送 agent 回来做的事，读一份约 300 词的文件，而不是整份技能。理由：重开一份 spec 编号会变、ticket 的 `## Parent` 指错；正文有历史则 user 读 spec 时在读历史。按步名指向这一步的调用方：`triage` 技能 `ready-for-agent` 一条的「extend a published one」、`to-tickets` 技能 `SKILL.md`、`write-screen-contract` 技能 `## Next`、`design-pages` 技能 `references/pull.md` `## Reached from here`、`dispatch` 技能 `references/night.md` 的 `contract` child 一段；本步改名要同步改这几处。上游自己加了修订已发布 spec 的步骤 → 收上游措辞，「正文干净、理由进评论」保留 |
| 开头 `Do NOT interview the user for facts` 那一句 | 我们改的：只禁问事实，并指向第 1 步那个唯一要 user 拍板的判断（`the one judgement you put to them`），免得跟分卷确认自相矛盾。上游重写这句 → 收上游措辞，把这个例外重新挂上去 |
| 模板里 `## Implementation Decisions` 的说明 | 我们改的：小节编号（`### 1.`），每条决定句末标出处（decision ticket 的 ticket number、ADR、research file 路径），没有出处的明写 `this spec's decision`；仓库 `CODING_STANDARDS.md` 里影响某个决定的规则写进它影响的那一小节并注明出自哪一节——worker 不读那份文件，它需要的规则经 spec 与票到达；路径规则收窄成 `no implementation file paths`，出处路径、测试目录、共享 contract 的位置必须写。理由：`to-tickets` 的 ticket 要用「第 N 节」指回，`## Read first` 要从小节里抄出处。上游重写这一节 → 收上游措辞，把编号、出处、路径三条接回去 |
| 模板里 `## Testing Decisions` 的说明 | 我们改的：首句是一句大白话，写测试从浏览器页面、HTTP 接口还是函数调用看结果——这是 user 唯一看得懂的那一层，seam 由 agent 定之后 user 只从这句知道定的是什么；第二句写第 3 步定下的 seam 与允许打桩的 external seams；之后按 test layer 列目录与 precedent，那个要抄的东西叫 `the precedent to copy`；末尾列提交前要跑的命令。理由：ticket 的 `## Seam` 从这里抄，而 `to-tickets` 第 4 步写的是 `the precedent it names`、`CONTEXT.md` 登记的正名是 `precedent`——同一样东西不给两个名字，否则写 ticket 的一方在 spec 里搜 `precedent` 搜不到。上游改这一节 → 收上游，大白话首句、seam 那一句、分层落点、`precedent` 一词四条保留 |
| 模板里 `## Testing Decisions` 的 **Critical flows**（关键流程） | 我们改的：原 **Cross-ticket flows** 收窄为关键流程——钱、登录、提交链；产品没有这类流程就写 `none`，读的人分得清「没有」与「漏写」；每条写流程名与涉及的 Implementation Decisions 节号。`to-tickets` 按行切 acceptance ticket。这一项只在 spec 有 screen contract 时写：`to-tickets` 只在切界面票时（`references/cutting-interface-tickets.md` 的 **acceptance ticket** 一节）读它，没有界面的 spec 写了它，没有人据此切出 acceptance ticket，而没有界面的 spec 也照常跑 night（MMW #374、#415、#424，agentflow #720、#913、#914）。另一个省略条件是产品永远不能整体启动（一个库、一个没有运行中产品的组件）；`.mmw/target.json` 还不存在的产品照写，因为它的 contract ticket 会落地 `start`。理由：#447 第 6 节（#415 第 16 节的范围收窄）；省略条件原先写「`.mmw/target.json` cannot start the whole product」，而新产品写 spec 时还没有 `.mmw/target.json`，照字面读每张 acceptance ticket 都被丢掉——按「从零做新产品」走真实流程时发现。没选的另一条路是让 `to-tickets` 对没有界面的 spec 也读这一项：acceptance ticket 的 journey、`--break` 与 break switch 都是 `ui-acceptance` 的机制，没有 screen contract 的产品没有它们。`to-tickets` 那一侧只按行切，条件只写在这里。上游改 `## Testing Decisions` → 收上游措辞，这一项保留，名字保持 **Critical flows** / 关键流程，两个省略条件保留 |
| `references/revising-a-spec.md` 第一段末句「A published spec, a ticket body and its acceptance criteria are edited only by the main agent or the user.」 | 我们加的。理由：#415 第 5 节。`## When the rows change` 原来末尾同义的一句随它挪进同一份文件时删掉，不在一份文件里说两遍。上游改修订步骤 → 收上游措辞，这一句接在修订那一段之后 |
| 模板里 `## Further Notes` 之前的 `## Sources` 节 | 我们加的：一手来源固定十二类（wayfinder `map`、Originating issue（`triage` 送来的外来 issue 与它那条 agent brief 评论）、decision ticket、上游 spec（包括决定票引为依据的旧 spec）、ADR、research file、prototype 目录、design package、screen contract、Domain docs、实测证据、仓库规则），每类无则填 `none`。仓库规则一类点名 `CODING_STANDARDS.md`（产品代码）与 `TESTING.md`（测试：分层、哪些外部边界可以打桩、怎么跑）两份文件和本 spec 依赖的节：worker 不读这两份文件，它需要的那几节经 spec 与票（`to-tickets` 模板 `## Read first`）到达；上游原来写 `Test rules (the repo's TESTING.md or equivalent)`。`implement` 技能靠这个节名往回读，改名要同步改 `implement`；`to-tickets` 的 `## Read first` 从这里按 ticket 挑。Originating issue 一类让外来 issue 在 spec 上有位置：`triage` 第 5 步在 spec 发布或修订后以一条链接评论关掉那张 issue，agent brief 只存在于 tracker 上（`docs/adr/0001-tracker-repo-authority.md`），取代它的 spec 靠这一行走回去；不把那张 issue 挂到 spec 底下，因为 spec 的每个 sub-issue 都被当成 ticket——`to-tickets` 第 8 步数 sub-issue、`verify-ticket.py` 的 `lint_spec` 逐个 lint。上游自己加了同类的来源节 → 用上游的名字，同步改 `implement` 与 `to-tickets`，十二类保留 |
| 开头「issue tracker 与 triage label 词汇没给你就去装」那一句 | host 中立：技能名写成散文形式，与 `triage`、`wayfinder` 的同一句同一个说法。共同理由见 [README.md](README.md#host-中立)，写法见 `writing-for-agents` 技能的 `SKILL-SET-REVIEW.md` `### Paths, tokens and host neutrality` 与 `### Hand-offs` |
| 第 2 步的第二段（screen contract）、`## Implementation Decisions` 说明里的 **API contract** 小节、`## Sources` 的 Design package 与 Screen contract 两类 | 我们加的：有界面的效果有两个各管一域的基线——design package 管外观与逐字文案，`docs/specs/<effort>/screen-contract.yaml`（`write-screen-contract` 技能写出；有 map 时从 alignment ticket，没有 map 时与 to-spec 同一 session，这一点正文只在两条退回路径里体现，不在合同的出处上再写一遍）管调用、显示值、流转、失败与计时。spec 读全合同；有 `gap` 未 `aligned` 就停、不出 spec：有 map 退回 alignment ticket，没有 map 留在同一 session、退回 `write-screen-contract` 的 **Reverse sweep**，再由用户在 **Write the gap list and stop for the user** 里裁决。没有 map 就没有 alignment ticket——两条退回路径已按有无 map 分开，正文不另写这一句。`calls`/`shows` 两列生成 **API contract** 小节，新项目的 OpenAPI 从这里起；把它变成 models 与 route signatures 的是切出来的第一张票，这条规则写在 `to-tickets` 的 `references/cutting-interface-tickets.md`，因为写票的 agent 读的是发布的 spec，不读本技能的模板。理由：design package 和后端决定各自完整、无处汇合，变色龙的界面因此接了空；没有 map 时没有 alignment ticket 可退（#446 第 4 节、#447 第 6 节）。上游改这几节 → 收上游措辞，这三处与两条退回路径接回去；Sources 若改名同步改 `implement`、`to-tickets`、`write-screen-contract` |
| 第 2 步「读全合同」加 `pages` 与 `scenes`；`## Implementation Decisions` 说明加「story 的定论折进实现它的小节」一段并把「user-story number」从出处清单拿掉 | 那一段不写 `write-screen-contract` 的 lint 对仍引 story 的行报警——那是另一个技能的行为，写 spec 的 agent 照规则写就不会触发。我们改的，来自 mmw #216 第 8 节（取代 #115 的 mechanism registry 与「只引 pages/scenes」的 visual acceptance；后两项的现行写法见下面 visual acceptance 与 How a test arrives 两行）。上游改这几处 → 收上游措辞，pages / scenes 与 story 定论折进实现小节保留 |
| `## Implementation Decisions` 说明里 **API contract** 之后的 **`App · ` 页组合** 小节 | 我们加的：有 screen contract 时的固定小节；每条 **cross-component row** 对应的请求字段与另一区域的状态，按 row id 引用。理由：#447 第 6 节。上游改 Implementation Decisions 说明 → 收上游措辞，这一小节保留 |
| `## Implementation Decisions` 说明里的 **visual acceptance** 一段 | 我们改的：外观由 `ui-acceptance` 的 story judge 按 `data-ui` id 做 **element parity**，引用合同的 `pages`，`App · ` 页包含在内；仍不写命令形状；「写在这里就是会漂移的第二份」这条理由不进正文。取代 #216「`App · ` pages excluded」与「只引 pages 与 story 判官」。理由：#447 第 6 节。上游改这一段 → 收上游措辞，element parity、pages、`App · ` 页包含、不写命令形状保留 |
| 模板里 `## Testing Decisions` 的 **How a test arrives at a state** 末句与 **Test surfaces** | 我们改的。**How a test arrives at a state**：有 screen contract 时点名三个到达机制，各写由哪张 ticket 建——从 scene data（或合同的 scene input）把 story 页摆进一个 scene 的 story adapter、按 `data-ui` id 把控件摆上屏给 four-column boundary test 用的 interaction helper、给 journey 起产品的 `.mmw/target.json` 的 `start`。新产品上 contract ticket 建 story service 与第一个 story adapter、interaction helper、`start`，之后每张 component page ticket 加本页的 adapter（与 `ui-acceptance` 的 `references/story-parity.md` 和词表一致）；`.mmw/` 已经能回答的产品，写出建缺失之物的那张票——缺的是 `target_config.py --check` 报的，加上为别一代 design package、scene 形状或控件查找方式建的答案——或写明什么都不缺。每个机制怎么搭写在 `ui-acceptance` 技能的 reference 里，这里只点名机制与主人、不复述形状。理由：`to-tickets` 第 4 步第 4 问要求把系统摆进某个状态的东西在这一节有名字、并落在某张票的 **Owns** 里，缺一样就切 `reach` ticket，第 8 步还回读核对；这一节对界面批次留空，整批界面判据都判第 4 问失败，而新产品的 contract ticket 正是从这份 spec 切出来的，写 spec 时没有别处可抄（#539、任务板试点 #541）。**Test surfaces** 按 `ui-acceptance` 技能的 `target_config.py --check` 打印的项列，每项 `ok` 或缺，不在这里列一份——旧的七项枚举把可选的 `instance` 算进去、漏了必填的 `harness_markers`（#447 第 6 节）；也不指写 spec 的一方去读 `references/product-answers.md`：`--check` 按名打印每一项，那份文件是建这些答案的 worker 读的。上游改这两项 → 收上游措辞，三个机制与主人、形状不复述、以 `--check` 打印的项为准保留 |
| `</spec-template>` 之后的 `## Next` | 我们加的：末节只点名下一步 `to-tickets`。有 map 时 wayfinder **Work through the map** 第 6 步已经指路，没有 map 时由这里接上——这句理由只写在这里。理由：#447 第 6 节、方案 K7。上游在 SKILL.md 末尾加了下一步 → 收上游措辞，`to-tickets` 保留 |
| 第 1 到 5 步各自末尾的 `Done when` 一行 | 我们加的：这份技能不到一半的行是上游的，按本仓自己的文本审，每一步要有一行完成判据。第 1 步：引用领到的来源都读过、知道这次写哪一份 spec；第 2 步：说得出 spec 碰哪些模块，有 screen contract 时每行 `gap` 都 `aligned`；第 3 步：行为所依赖的每个状态都有观察它的 seam，并有经这个 seam 到达它的办法或一个点了名的机制；第 4 步：spec 带 `mmw:spec` 发布，从 map 来时读回的 native `parent.number` 是 map；第 5 步（在 `references/revising-a-spec.md` 末尾）：正文读来像一开始就这么写、说明改动的评论已发、从这一节切出的票都与新文本相符。上游给步骤加了完成判据 → 收上游措辞，没有覆盖到的这几行保留 |

### docs page

`mmw-v2/upstream/docs/engineering/to-spec.md` 加了两处：`## Common questions` 里「I just finished a wayfinder map」一问之后一段，说在 GitHub 上从 map 发布的每份 spec 都建成 map 的 native sub-issue、读回后才报完成，而不从 map 来的 spec 不编一个 map parent；`## It's working if` 末尾对应的一条。与 `SKILL.md` 第 4 步 native parent 那三段同一行为，写给读说明页的人。上游改这两处所在的段落 → 收上游措辞，这两处接回去

### agents/openai.yaml

| 字段 | 我们的意图 |
| --- | --- |
| `policy` 整块（`allow_implicit_invocation: false`） | 删掉。跟 `SKILL.md` 的 `disable-model-invocation` 同步去掉，两处必须同增同删 |

## 一个概念一个名字：product answers / acceptance runtime

`## Testing Decisions` 的 **Test surfaces** 那一条原来写 `the answers that make the repository a runnable environment`。同一样东西在工具箱里有过四个名字——`automatable acceptance runtime`、`product answers`、`runnable environment`、`drivable`——而写 spec 的 agent 被这一句送去 `ui-acceptance` 的 `references/product-answers.md`，打开却是第四个说法，分不清是不是同一个文件。定名：**内容**叫 `product answers`（它是那个 reference 的文件名，也是 ADR 0028 的用词），**状态**叫 `acceptance runtime`；两个都登记在 `docs/contexts/ui-acceptance/CONTEXT.md`。这一条因此写成 `the **product answers** that make the repository an acceptance runtime`。

上游改 **Test surfaces** → 收上游措辞，`product answers` 与 `acceptance runtime` 两个名字保留，不收回 `runnable environment`。

## 没有合同行的决定；外观验收那一段放哪

第 2 步加一句：没有合同行、design package 也没画成控件的决定（例如一段文字的行为），写进它所属的小节，写成 "this spec's decision" 并注明它依据什么，与模板 `## Implementation Decisions` 说明里没有出处的决定同一个写法。这一句排在两条退回路径之后：放在「`gap` 未 `aligned` 就停」与退回路径之间，退回路径会被读成这一句的后果。模板的 visual acceptance 一段写明放在自己的编号小节或带 `data-ui` id 的那一节。理由：任务板试点 #541 起草 spec 时两处都要猜。上游改这两处 → 收上游措辞，这两句保留。

## Critical flows 每一行的形状

模板 `## Testing Decisions` 的 **Critical flows** 一项加两句：每条流程一行，嵌在这一项下面，写成 ``- `<flow>`: Implementation Decisions sections <n>, <n>``；`Implementation Decisions` 这两个词不论 spec 用什么语言写都保持英文，与票的 `## Parent` 点名小节的写法一致，节号跟在它后面（例如 `Implementation Decisions 第 2、4 节` 也读得出，正文把这个值标成示例）；`verify-ticket` 技能的 `--lint` 读这些行来判定哪些 journey 必须带 `--break`，读不出的行报 ERROR 并给出这个形状。

理由：任务板试点 #541 的 spec #555 用中文写成 ``- `settings-save`：…；第 6、8、9 节``，`critical_flows()` 只认 `Implementation Decisions` 这两个词，读不出节号；它又只在下一个以 `- **` 开头的行才停，把后面一条普通 bullet（「提交前运行」）也当成流程行报错。脚本那一侧改成在这一项自己的列表结束处停；接受的形状只留一种，写在这里，ERROR 原样引用它。上游改 `## Testing Decisions` 的这一项 → 收上游措辞，这个形状与「节名保持英文」保留。
