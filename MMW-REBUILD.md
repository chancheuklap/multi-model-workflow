# MMW 重建 · 规格与生长记录

新插件不改造旧插件。它在一个新目录里从空白长出来，旧的 `plugin/` 原地不动继续可用，直到新的跑通一轮真实任务。

本文件是新插件的规格来源，随重建逐层生长。每层定完写进这里，之后写代码只照这里，不再回到讨论。配套的可视化面板与本文件同步更新，那里画形状，这里给精确值。

## 材料来源

这是重建，不是新造。三类材料各有各的用途，不得混用：

| 材料 | 用途 | 纪律 |
| --- | --- | --- |
| 旧 `plugin/`（技能、references、脚本、routes.json） | **内容的唯一来源**：每一格干什么、每份技能的方法、每条纪律 | 写进新插件的每一句都要能指回旧文件；标出处 |
| 那份 artifact 与 `docs/design/MMW-SIMPLIFICATION.md` | 取舍依据：删什么、合并什么、为什么 | 只用于决定去留，不用于生成内容 |
| 宿主官方文档与本机实测 | 宿主能力事实：frontmatter 字段、参数机制、调用控制 | 承重结论必须实测或引文档，不凭印象 |

推理只用来做取舍，不用来生成内容。凡是三类材料里都查不到的说法，就是编的，不写进规格。

## 范围

本次只做 Claude Code 一家：不切核心区与适配区，不写能力声明，不做跨宿主共享验收测试。四宿主的抽象边界等这份干净实现稳定跑过之后，拿第二家宿主的真实迁移实测出来。

背景资料在 `docs/design/MMW-SIMPLIFICATION.md`（决策档案，遇到具体分叉去查它，不按它的十六步执行）。

## 四层顺序

| 层 | 内容 | 完工判据 | 状态 |
| --- | --- | --- | --- |
| 一 · 地基 | 状态五字段、九格阶段、七个入口 | 后面每一层都能从它机械推出，不再有判断空间 | 本文件第一节 |
| 二 · 骨架 | 全部技能、角色、钩子、检索工具的文件建出来，每份只有头部声明加一句职责 | 目录树完整可见，不含任何方法论 | 本文件第二节 |
| 三 · 接线 | 状态读写、切格、增量审基准写进技能正文由主线程执行；派发脚本与出站钩子实现 | 能空转一整轮：切格、派发、落盘都通，被派者拿到的提示词仍是空壳 | 本文件第三节 |
| 四 · 灌方法论 | 一份技能一份技能地从旧插件提取方法、剥掉流程仪式 | 逐份验收，三问过关 | 待定 |

第四层的逐份验收三问：方法有没有丢、机制性仪式有没有跟着搬进来、有没有路径依赖。

## 每份技能与角色文件都画一条线

文件里一条 `## 线下 · 不是技能内容` 把内容切成两段，31 份全有。标题下不再重复解释它是什么——那句解释逐字抄 31 遍，标题本身已经说清楚了。

**线上只放干活当下要用的四类**：动作（做什么、什么顺序）、判据（怎么判断走哪条、什么算合格）、硬约束（不许做什么、必须先做什么）、落地位置（产物写到哪、改状态哪个字段）。

**线下放五类**：为什么这么设计、宿主机制的成因、历史（旧插件怎么做的、改过几版）、元信息（这份属于哪一层、换宿主要不要重写）、脚本运行时自己会说的话。施工单也在线下。

**唯一例外**：某条纪律没有机器拦着、模型又可能自作聪明跳过时，允许跟半句「否则会怎样」。上限半句，同一件事只说一次。

线上区另外三条，逐句过一遍就能查出来：

| 查什么 | 长什么样 | 怎么改 |
| --- | --- | --- |
| 模型默认就会做的话 | 写了不改变行为 | 删掉整句，别改词 |
| 同一意思出现两处 | 开头一句纲领与下面的步骤互相复述 | 留信息最全的那处 |
| 靠禁止来指路 | 「不是 A 而是 B」「不要 X」 | 改写成正面动作；只有当禁令是硬围栏、且配了替代做法时才留 |

**模糊指代要按名字写死**：「那份技能」写成 `mmw-review`，「三份文档」写成计划、设计、切片，「仓库根那份文件」写成 `TESTING.md`。读的人眼前只有当前这一份，代词指不到任何东西。

**每份文件只对一个读者说话**：给被派者读的技能里不写「怎么派它」，那是主线程的事，写进主线程读的那一份。

这条线是第四层的自查工具：填完一份，线下比线上长就是写偏了。现在骨架期线下普遍略长于线上（施工单占七八行，线上还只有一句职责），填完肉应当反过来。

---

# 第一层 · 地基

## 1.1 九格阶段

格子是可跳的，不是必经序列。不敲命令就停在原地。

| 键 | 名 | 干什么 | 位置 |
| --- | --- | --- | --- |
| `wayfind` | 探路 | 把大事项拆成决策地图，逐个解开 | 主干 1，开任务落点 |
| `investigate` | 查清现状 | 并行投查，出带引用的现状报告 | 主干 2 |
| `propose` | 给方案 | 两三个方案，用户挑一个 | 主干 3 |
| `design` | 做设计 | 谈、做原型、按反应改文档，循环到用户认可 | 主干 4，**驻留格** |
| `to-issue` | 切片 | 设计切成能独立认领的活 | 主干 5 |
| `plan` | 写计划 | 换个模型写，主线程核 | 主干 6 |
| `build` | 落地 | 派工人写码，测试先行 | 主干 7，常回主干 4 |
| `package` | 出包 | 打成能装的安装包 | 主干 8 |
| `closing` | 收尾 | 合并、清工作树、摊旁路清单 | 主干 9，终点 |

阶段键是机器标识，用户敲命令时中文名与英文键都认。

**探路是主干第一格，开任务默认落在这里。**负责人的实际节奏是每个新任务几乎都要先探路——终点大致明确，但连要决定什么都还说不准。要决定什么一眼就清楚的小改动，敲一次往下走直接跳过去。

场景预设取消：小改动就是开在探路然后直接跳到落地，不另设一套开口。

### 1.1.1 三段节奏

九格不是均速走完的九步，是三段不同密度的活。这决定了哪些格要为长期驻留设计、哪些格要为代理自跑设计。

| 段 | 格 | 谁在盯 | 停留 |
| --- | --- | --- | --- |
| 想清楚 | 探路 → 查清现状 → 给方案 | 人机密集 | 按事项大小 |
| 定下来 | **做设计** | 人机密集，反复原型与界面稿 | **整个任务里最久的一段** |
| 干出来 | 切片 → 写计划 → 落地 | 几乎无人值守 | 由代理连着跑完 |
| 收回来 | 出包 → 收尾 | 回到人 | 短 |

**落地回设计是主路上的一条边，不是异常。**做完一轮落地拿到能跑的东西，回头改设计，再走一遍切片、写计划、落地——这个大环可能转好几圈，直到确认没问题才往出包走。

因此有两条结构性推论。一是设计格必须按驻留设计：一份持续被改的设计文档加一串原型轮次证据，不是「产出一份文档就走」。二是干出来那一段的三格必须能让代理自己敲推进，代敲纪律（决定与理由追加进 `note`）主要服务这一段。

**判据：能停在那儿的才是阶段。** 收尾是阶段，因为工作树与状态都还在，可以停到下周再合并；开任务不是阶段，它执行完状态就已经是探路，而且执行之前连状态文件都无处可写。由此推出一条纪律：**开任务要快，不要在无状态区久留**。敲 `/mmw-start <需求>` 之后聊十轮才建工作树，那十轮全在盘外，关掉会话就没了。需求理解到能起个名字就动手建树，深入的对话放进第一格做。

技能份数因此多于九格：九份对应九格，其余对应动作（开任务、起审、收尾）。

## 1.2 推进规则

`/mmw-next` 的全部行为，无判断：

1. 读状态文件取当前格。
2. 目标格等于参数指定的格；没给参数就取主干下一格。
3. 写回状态文件。
4. 打印目标那一格的技能名，以及本任务的约定路径。

`/mmw-back` 方向相反，同样不校验，**只多一条**：目标格是 `design` 时把过门标记清回空，并提示改完要重新过门。

**唯一拒绝点**：当前在 `design` 且状态里没有过门标记时，拒绝往下走，提示先敲设计过门。除此之外任何格之间的跳转都不校验、不问理由、不记账。

两条合起来，回设计改完必须重新过门这件事就自动成立了，不必给设计文档算哈希、也不必让用户交代这次回去是看一眼还是要动手——**目标格自己就是意图**。回查清现状、回给方案不碰设计文档，标记照旧。

## 1.3 状态文件

位置 `<任务工作树>/.mmw/task.json`，格式 JSON。目录名不带宿主标记，同一个工作树换宿主打开就能接着做。`.mmw/` 加进仓库忽略清单——这是整套插件唯一一处往用户仓库写内容的地方。

字段判据：这个值没有别的地方能推出来。

| 字段 | 类型 | 存什么 | 何时写 |
| --- | --- | --- | --- |
| `task` | string | 任务名，贯穿工作树、分支与文档目录 | 开任务时一次 |
| `phase` | string | 当前格的键 | 每次推进或回退 |
| `base` | string | 起点提交完整 sha | 开任务时一次 |
| `note` | string | 一句现场注记；无人值守时代理代敲的决定与理由追加在这里 | 随时 |
| `design_approved` | object \| null | `{ at, docs[] }`：过门时间与当时认可的文档清单 | 过门时写；回退到设计格时清回 null |

```json
{
  "task": "mmw-rebuild",
  "phase": "design",
  "base": "5f79af59e8f838825423ef0b17ec7bb13532482f",
  "note": "原型第三版待你看；上次停在配色没定",
  "design_approved": null
}
```

不存的东西分两类。**删掉的**：返工计数、缺陷指纹集、指纹重复次数、掉头台账、掉头计数、值守档、无人值守策略、阶段序列、阶段游标、步骤游标、审闸标记、状态字、历史流水、入口能力、入口证据、产出登记、结论词、场景预设——它们全在回答同一个问题，机器该让你去哪一步，而这个问题不再由机器回答。**搬家的**：审查基准搬进审查记录，执行步账从提交流水推导，产物路径靠 1.6 的约定推导。

开任务顺序固定为先建工作树再写状态：状态没有地方存之前不能开任务。主仓库因此不需要在飞任务索引，`git worktree list` 就是答案。任务结束删掉工作树，状态随之消失。

## 1.4 旁路清单

同目录第二份文件 `<任务工作树>/.mmw/sidelines.md`，纯 markdown，无 schema。

干活途中撞到本次范围外的问题记一行：一句话、在哪个文件、谁报的。收尾时摊给用户挑哪些开成远端 issue，一次性开完。不每发现一条就开一条——往远端写是出站动作，逐条弹确认太碎，而且落地阶段发现的东西常在后面几轮自己消解。清单随工作树一起删。

## 1.5 七个入口，全部是技能

**宿主事实（官方文档与本机样本实测）**：Claude Code 已把自定义命令并入技能，`commands/x.md` 与 `skills/x/SKILL.md` 都产生 `/x`，行为相同；技能是超集，多出支撑文件目录、调用控制与自动加载。技能自身支持参数与自动补全提示。因此**不为任何入口再套一层命令壳，`commands/` 目录不用**。

| 可用能力 | 写法 |
| --- | --- |
| 接参数 | `$ARGUMENTS` 全量；`$0` `$1` 按位；frontmatter 声明 `arguments: [name]` 后用 `$name`。正文没写占位符时，参数被追加成 `ARGUMENTS: <输入>` |
| 补全提示 | `argument-hint` |
| 谁能调 | `disable-model-invocation: true` 只有用户能调；`user-invocable: false` 只有代理能调并从 `/` 菜单隐藏 |
| 其他 | `allowed-tools`、`disallowed-tools`、`model`、`context: fork`（技能跑在自己的子代理上下文里） |
| 代价 | 技能正文加载后跨轮次常驻，每行都是重复 token 成本 |

| 入口 | 参数 | 做什么 | 谁能调 |
| --- | --- | --- | --- |
| `/mmw` | 无 | 报现状：在哪一格、盘上产物说到哪、上次为什么停。新会话的断点续传入口 | 用户 |
| `/mmw-start` | `<需求>` | 开任务：起名、建工作树、写状态、进去 | 用户（`disable-model-invocation`） |
| `/mmw-next` | `[阶段]` | 往下走 | 用户；无人值守时代理自调即代敲 |
| `/mmw-back` | `[阶段]` | 回上游 | 用户；代理可自调 |
| `/mmw-review` | `[阶段]` | 起一道审，审那一格的主产物 | 用户；代理可自调 |
| `/mmw-approve-design` | 无 | 设计过门，写事实与文档清单，不算哈希 | 只有用户（`disable-model-invocation`） |
| `/mmw-done` | 无 | 合并回主分支、摊旁路清单、清工作树 | 用户（`disable-model-invocation`） |

**整套只有一种变量：阶段名。**往下走、回上游、起审共用它，取值就是九格自己的名字，不另记代号，且三处都可省略——不填即当前格或主干邻格。

开任务不与报现状共用一个入口。靠有没有参数区分两种语义是不必要的重载，拆开后 `/mmw` 只剩断点续传一个用途。

代理无权代敲的四处：设计过门、缺用户才能给的输入、往远端推送、起终审。前三处有机器保证（前两处靠 `disable-model-invocation`，推送靠出站钩子或宿主权限框）。**起终审没有机器保证**：它与设计审、计划审共用同一份起审技能，而无人值守时代理必须能自起那两道，所以这份技能不能关掉模型调用；终审要人起只能写在技能正文里，属于提示词约束。代理每代敲一次，把决定与理由追加进 `note`。

### 1.5.1 起审时审什么

**一格一种审**：敲哪一格就审那一格的主产物。不另立审查场景的词汇，也不需要代理在同一格里挑对象。

| 敲哪一格 | 审的是 | 谁来审 |
| --- | --- | --- |
| `design` | 设计文档。它当前引用的原型证据一起看 | 派第二模型：设计由主线程与用户谈出 |
| `plan` | 计划文档 | 主线程这一族：计划由第二模型写 |
| `build` | 全量改动，即终审 | 两路都派：一路对着设计与计划逐条核，一路不看计划、全新眼光独立审计 |
| 其余格 | 该格按 1.6 约定的产物 | 走通用审查方法，无专门配置 |

不变量是**写者与审者不能是同一个**，不是「审者必须是第二模型」。

**还有第四道审，不占这个入口：工人交活时主线程当场审那一份。**代码是工人写的，主线程本来就是另一个模型，不变量已经成立，不必再派人；而且它手上有设计与计划的完整上下文，比干净上下文的审者判得准。它由代理自己跑，但**同样落进审查记录、同样记基准**——否则终审判增量时基准是断的。它的价值是当场发现比攒到最后便宜，**不是让终审可以少读**：终审那一路独立审计的价值全在于不受任何在先判断影响，略读就把它抹掉了。

设计格没有「成文前后」之分，起审永远审那份设计文档；只想审某一版原型验得对不对时，敲入口时用自然语言补一句，不为它设第二个变量。

**设计文档的成型顺序已定**：进设计格时就有初版，原型与界面稿一轮轮完善它。旧 `plugin/state-schema/routes.json` 那条线性四步（讨论 → 做原型 → 按模板成文 → 自检）不采纳，因为它把成文摆在最后，与「长期驻留、反复迭代」的实际节奏对不上。自检不是末尾一道工序，是每次改完文档都过一遍。第四层写设计格技能时按这个顺序取旧材料。

**工人交活那道快审不走这条命令**。落地一般无人值守：工人每交一份，主线程当场快速简短审一份，这是落地方法论里的内嵌步骤，由代理自己跑。用户手动敲 `/mmw-review 落地` 的时机只有一个——所有计划全部落地之后起终审。这也是代理无权代敲终审的原因：终审是整个任务质量的最后一道，代理自己起自己收等于自己判自己及格；落地跑完本身就是天然的交接点。

增量还是全量不占变量位：代理读审查记录里的基准句加提交流水自行判定，判不出来退回全量审并告知用户。

谁来审、几路视角、各看什么角度的细节，属于角色配置与方法论，在第二层与第四层落定。合并几个分支回主分支那道审由合并技能自己驱动——它发生在任务之外，状态文件都不存在。

## 1.6 产物路径约定

登记删掉之后，路径全部从任务名拼出来。这张表就是约定的全文。

| 哪一格的产物 | 路径 | 随分支提交 |
| --- | --- | --- |
| 探路的决策地图 | `docs/design/<任务名>/wayfind/` | 是 |
| 现状报告 | `docs/design/<任务名>/investigating.md` | 是 |
| 选定的方案 | `docs/design/<任务名>/direction.md` | 是 |
| 设计主文档 | `docs/design/<任务名>/<任务名>.md` | 是 |
| 原型与逐轮账本 | `docs/design/<任务名>/prototype/` | 是 |
| 切片 | `docs/issues/<任务名>/` | 是 |
| 计划 | `docs/plans/<任务名>/` | 是 |
| 审查留痕与终审报告 | `.mmw/reviews/` | 否，过程产物 |
| 可写角色的工作树 | `.mmw/worktrees/<任务名>-<片名>` | 否，验收后 `--no-ff` 合回任务分支再清掉 |

设计是一个文件夹，主文档与文件夹同名，方案、现状、原型都在它下面：隔一个月回来打开一个目录就看得全。

原型那个目录内部还有一套分区与三条硬规矩，是从一个真跑了十一轮的原型目录里读出来的，记在 `mmw-prototype` 的施工单里，第四层填正文时落地。设计驻留期越长这套越要紧。

## 1.7 本层边界

已经定死：九格的名字与主干顺序、探路是第一格、设计是驻留格且落地常回它、状态五字段与文件位置、六个命令的语义与指向、八条路径约定、唯一拒绝点与回设计清过门这两条状态规则。

故意没碰：每份技能里写什么方法（第四层）、有哪些子代理角色与各配什么模型（第二层）、怎么派子代理与收结果（第三层）、审查循环的具体跑法（第四层）、另外三个宿主怎么迁（本次范围之外）。

---

# 第二层 · 骨架

新插件落在仓库根的 `mmw/`。名字不带宿主前缀：旧的四份是「某宿主的 plugin」，这一份就是 MMW 本身，与状态目录 `.mmw/` 同名。旧 `plugin/` 原地不动继续可用。

骨架层只建文件、不写方法论。每份文件有头部声明、一句职责，以及一张**施工单**——来源哪个旧文件、保留什么、删除什么。第四层填肉时照施工单办，不再重新判断。

当前规模：36 个文件、约 1050 行（25 份技能、6 份角色、2 个钩子文件、2 个脚本、1 份插件清单）。旧 plugin 是 14845 行，其中 6709 行是保脚本的测试。

## 2.1 入口（7 份技能）

| 新技能 | 来源 | 删掉的 |
| --- | --- | --- |
| `mmw` | `commands/progress.md`、`commands/reassess.md`、`scripts/progress.sh`、`flow.sh` 的 where | 进度板渲染器与投影层；where 的指路指令面 |
| `mmw-start` | `scripts/prepare.sh`、`routes.json` 的 entry_policy | 入口能力判定与原话取证；场景预设选择；三十九字段任务档案 |
| `mmw-next` | `flow.sh` 的 handoff 与阶段推进 | 推进引擎、阶段序列与游标、五个结论词、审闸强制、打转守卫、掉头台账 |
| `mmw-back` | `flow.sh` 的 needs-redirection | 掉头计数与方向横跳判据；讨论态与流水线态区分 |
| `mmw-review` | `references/review/review.md`、`scripts/review.sh`、`review/plan-impl.md` | 派发指南先落盘再读回；审闸标记与强制进闸；跨计划合同门单列 |
| `mmw-approve-design` | `commands/approve-design.md`、`scripts/steer.sh` 的 approve | 文档指纹与哈希比对；过门自动切值守档并推进 |
| `mmw-done` | `references/closing.md` | 钉产出与交还引擎；遗留项与旁路两套登记 |

## 2.2 阶段方法（9 份技能）

| 新技能 | 来源 | 删掉的 |
| --- | --- | --- |
| `mmw-wayfind` | `references/wayfind.md` | 与引擎的交接与结论词 |
| `mmw-investigate` | `references/investigate.md`、两份 workflow 脚本 | 自建并行编排（并行是把派发第一步连着调几次）；检索候选 JSON 管道 |
| `mmw-propose` | `references/propose.md` | 看引擎 do 判降级的开关 |
| `mmw-design` | `design/discussion.md`、`design-doc-template.md`、`design-self-check.md` | 方法顺序里嵌的引擎调用与值守档；取证战役单列 |
| `mmw-prototype` | `design/prototype-mockup.md`、`scripts/prototype.sh`、`lib/prototype-state.sh` | 三状态机与轮次计数、日志文件、下游材料校验；账本里的哈希锚点 |
| `mmw-to-issue` | `design/to-issue-skeleton.md` | 上游产物登记与接力单 |
| `mmw-plan` | `plan/plan-flow.md`、`plan-self-check.md` | 并行派发的引擎步账；交还后强制进审闸 |
| `mmw-build` | `build-a.md` + `build-b.md` 合一 | 按场景分两份（判据改成改动面）；执行账本步账与断点恢复表 |
| `mmw-package` | `references/package.md` | 出包状态机的状态词表与五个引擎子命令面 |

`mmw-prototype` 从设计里拆出来，因为它是另一门手艺：一个是把结构想清楚，一个是做一版让用户看了能说「不对，应该这样」。收尾那一格没有单独的技能，`mmw-done` 同时是它的方法。

## 2.3 角色方法论与共读（7 份技能）

| 新技能 | 谁读 | 来源 |
| --- | --- | --- |
| `mmw-worker` | 写码工人 | `worktree-build/SKILL.md`、`discipline.md`、`when-stuck.md` |
| `mmw-plan-writer` | 计划撰写者 | `worktree-plan/SKILL.md`、`plan-self-check.md` |
| `mmw-reviewer` | 审者 | `worktree-review/SKILL.md` 与它的五份 references |
| `mmw-task-pack` | 写计划的人与核计划的人 | `plan/task-pack.md`（旧仓库里有两处完全相同的复制） |
| `mmw-testing` | 计划撰写者、工人、终审者 | `worktree-build/references/tests.md` 的测试质量片段、仓库根 `TESTING.md` |
| `mmw-retrieval` | 任何要查结构的人 | `references/retrieval-doctrine.md` |
| `mmw-dispatch` | 主线程，要派角色出去时 | `scripts/worker.sh` 的派发与恢复、`scripts/review.sh` 的派发指南 |

这七份都设 `user-invocable: false`：它们是背景知识，不是用户敲得出来的动作。

`mmw-dispatch` 是这批里唯一的适配层文件，见 2.5。

## 2.4 独立入口（2 份技能）

| 新技能 | 来源 | 为什么独立 |
| --- | --- | --- |
| `mmw-release` | `release-flow/SKILL.md`、`drive-loop.md` | 零宿主耦合、零阶段耦合，与主干的唯一接点是出包那一格调它 |
| `mmw-merge` | `scenario/merge.md`、`worktree-review/references/merge.md` | 发生在任务之外，不建工作树也没有状态文件 |

## 2.5 角色（6 份，全在产品层）

**修正一个之前的错判**：曾把角色分成「宿主内」与「派给第二模型」两类，那个分法是错的。它把 Claude Code 的单模型限制当成了产品结构。

真实情况是宿主分两类。Claude Code 与 Codex CLI 是**模型专用宿主**，只能原生派自家模型的子代理，要用另一家模型必须起无头命令行。Cursor、pi、Droid 是**多模型宿主**，原生就能在角色文件里给不同子代理指定任意模型，六个角色在那边全是普通角色文件，根本不存在「第二模型侧」这回事。

所以「要不要起无头命令行」是本宿主的一条限制，属于适配层，不该渗进角色定义。六个角色一律住 `mmw/roles/`，格式统一，宿主无关。

| 角色 | 模型 / 思考档 | 权限 | 方法论 |
| --- | --- | --- | --- |
| `reviewer-claude` | Claude 推理型 / high | 只读 | 技能 `mmw-reviewer` |
| `reviewer-gpt` | `gpt-5.6-sol` / high | 只读 | 技能 `mmw-reviewer` |
| `scout` | 常规档 | 只读 | 技能 `mmw-retrieval` |
| `plan-writer` | `gpt-5.6-sol` / high | 只写自己那份计划与对应切片 | 技能 `mmw-plan-writer` |
| `executor` | `gpt-5.6-terra` / high | 写源码，文档目录禁碰 | 技能 `mmw-worker` |
| `executor-capable` | `gpt-5.6-sol` / medium | 同上 | 技能 `mmw-worker` |

审者按模型分两份，是因为产品要求终审两路视角、且写者与审者不能是同一个模型——这是产品事实，不是宿主事实，所以名字里带模型家族是对的，迁到多模型宿主这两份原样不动。

两档落地共用一份方法论，差别只有模型与思考档。选哪档读计划文档复杂度那一行，不靠派活的人临时拍。

### 角色文件的字段：哪些五家一样，哪些要在安装时注入

把四家现有实现的角色文件逐个比过，结论是差异面比想象的小：

| 概念 | Claude Code | Codex | pi | Cursor | Droid |
| --- | --- | --- | --- | --- | --- |
| 名字、说明 | 一字不差 | 一字不差 | 一字不差 | 一字不差 | 一字不差 |
| 用哪个模型 | `fable` | `haiku` | `openai-codex/gpt-5.6-sol` | `gpt-5.6-sol[effort=medium]` | `custom:GPT-5.6-Sol-[Codex]-0` |
| 思考档 | 独立字段 | 无 | 独立字段，另一个名字 | 并进模型名 | 并进模型名 |
| 工具白名单 | 竖排列表 | 方括号数组 | 逗号串 | 逗号串 | 方括号数组 |
| 能不能写盘 | 靠工具清单暗示 | 同左 | 同左 | 有专门字段 | 靠工具清单暗示 |

所以角色文件写共同那一层，用中性的词：`name`、`description`、`model`、`effort`、`write`（能不能写盘）、`skills`（方法论在哪份技能里）；可写的三份另加可写边界，`allow-paths` 是白名单、`deny-paths` 是黑名单。拼法差异留给各家的安装环节去翻——本宿主原生注册那两份因此另有一段 `tools`，那是本宿主自己的工具白名单写法。

`write` 这个字段值得单说：它是产品事实，五家都成立，Cursor 甚至原生就有对应字段。派发脚本把它翻成本宿主的沙箱档位。不把某一家的词（比如 Codex 的 `sandbox`）写进角色文件——那是让适配层的词汇倒灌进产品层。

### 角色怎么拿到自己的方法论：三层，别搅在一起

这件事反复改了几轮，根因是把三件事当成了一件。分开之后各自只有一个答案：

| 要解决的 | 谁负责 | 换宿主 |
| --- | --- | --- |
| 这个角色读哪几份手册 | 角色文件的 `skills` 字段 | 不改，产品事实 |
| 让那几份在被派者那一侧存在 | Claude Code 靠角色文件自动带；Codex 靠 `install.sh` 装一次 | 装机时适配一次 |
| 派活时让它真去读 | 提示词开头点名，派发脚本从 `skills` 字段自动补 | 不改，每家都成立 |

**第三层为什么必须有**：宿主的角色文件字段只保证被派者拿得到，不保证它去读；而无头那一侧压根看不见我们的角色文件。实测 `codex exec` 的全部选项里没有任何一个能为这次派发绑定技能，它只有一个全局技能目录靠自动发现——所以「找出每家的原生绑定方式、装机时注入」这条路在这里是断的。

点名是每家宿主都做得到的唯一一件事，因为每家都能写提示词。原生那一侧多说一句是零成本的重复，无头那一侧不说就是彻底没有。**六个角色、所有宿主，一律点名。**

**陌生字段会不会弄坏文件：实测不会。** 造一份塞了五个它们绝不认识的字段（`write`、`allow-paths`、`deny-paths`、`skills`、`sandbox`）的角色文件，分别放进 Cursor、pi、Droid 的角色目录：Cursor 与 pi 都派成功、子代理正常回话，Droid 能列出该角色。pi 那一条最硬——放错目录时它明确报「Agent not found」，放对之后错误变成「模型没有凭据」，说明它已经解析完整个文件并读到了模型字段，一次都没抱怨那五个字段。头部是 YAML，宿主只挑自己认识的键读，剩下的没人问津。

**真正会当场失败的是 `model` 的值**：pi 要 `openai-codex/gpt-5.6-terra` 这种带厂商前缀的写法，Claude Code 直接写 `haiku`。所以要在安装时翻译的是模型名，不是字段本身。

### 装在模型专用宿主上时，这套东西是两半

Claude Code 与 Codex 的插件结构几乎一样：都是 `agents/`、`skills/`、`hooks/`、`hooks.json` 加一份清单，只有清单目录名不同。这意味着同一棵树可以同时是两侧的源，各装各认得的那一半：

| 装在哪 | 装什么 | 谁装 |
| --- | --- | --- |
| 主线程这一侧 | 编排技能、两份原生角色、出站钩子 | 宿主自己按插件清单装 |
| 派发后端那一侧 | 被派者要读的方法论技能 | `scripts/install.sh` |

装哪几份到后端不另维护名单——六份角色的 `skills` 字段合起来就是它，安装脚本从角色文件读。用软链不用拷贝，改了插件里的技能两侧同时生效。装好之后提示词按名字叫技能就行（「读你已装的 `mmw-worker` skill」），不传路径；实测无头 Codex 能按名字自动找到并读出内容。

派发脚本开工前会检查后端装没装，没装当场停并指向安装脚本。多模型宿主没有「另一半」：六个角色全是原生角色，技能跟插件一起加载，那边根本不需要这个脚本。

**宿主怎么让六份角色生效，是一行清单加一份技能**：

- 插件清单点名两份 —— `"agents": ["./roles/reviewer-claude.md", "./roles/scout.md"]`。宿主官方文档明写这个字段接受单个文件路径的数组，不必是整目录。模型是 Claude 家的那两份因此被注册成原生子代理，模型、思考档、工具白名单由宿主强制执行。其余四份宿主不认，也就不会误注册。
- 技能 `mmw-dispatch` 加脚本 `scripts/dispatch.sh` 管另外四份 —— 技能讲判断规则（看角色的 `model` 是不是本宿主自家的）与什么时候派谁，脚本负责把命令行拼对。**两者都是适配层，换宿主整份重写**，多模型宿主那边技能会短得多、两个脚本都不需要。

三份角色方法论技能里因此各只剩一句「角色定义在哪、怎么跑起来见 `mmw-dispatch`」，没有一行宿主特定的命令。

对旧 plugin 的改动：这些参数原本埋在派发脚本的环境变量默认值里（`worker.sh` 三处、`review.sh` 一处），改一个模型要读 shell。现在角色是角色、派发是派发，各自一份文件。

## 2.5.1 宿主无关的写法纪律

本次只做 Claude Code，但每一处落笔都要让下一家宿主的迁移只改适配层。判据是**换一家宿主时这份文件要不要重写**。

| 层 | 内容 | 换宿主时 | 现在有多少 |
| --- | --- | --- | --- |
| 产品层 | 24 份阶段与方法论技能、6 份角色 | 角色里的模型 id 与工具名可能要改，正文不动 | 30 个文件 |
| 适配层 | 插件清单、钩子注册与红线脚本、`mmw-dispatch`、派发脚本与安装脚本 | 整份重写 | 6 个文件 |

由此推出三条硬纪律：

**一、宿主的限制不许渗进产品层。** 判断某件事属于哪层，问它在多模型宿主下还成不成立。「审者要两路不同模型」成立，是产品；「派 GPT 要起无头命令行」不成立，是适配。

**二、逻辑写进 Markdown，不写进脚本。** 状态读写、切格、派角色、收结果、判增量基准——全部由主线程照技能正文做。它读得懂 JSON、开得了工作树、跑得了命令行。旧 plugin 把这些封进 shell，代价是四家宿主各维护一套接线，还要 6709 行测试去保它们。

**三、脚本要新增须有硬理由，理由是「机器比模型准」或「主线程做不到」。** 当前三个：

| 脚本 | 硬理由 |
| --- | --- |
| `hooks/guard-outbound.sh` | 必须在工具调用发生之前拦截，主线程没法在自己发出命令前拦自己 |
| `scripts/dispatch.sh` | 无头派发参数多、错一个就白跑一轮，沙箱拼错等于围栏失效；越界核查要逐文件比对 |
| `scripts/install.sh` | 装机时的机械动作，名单从角色文件推导，人抄会漏 |

安装的验收判据只有一条：**装完之后，用户根目录里没有任何需要手工维护的本项目文件。**用什么机制装不重要——宿主原生能覆盖多少就用多少，覆盖不到的由安装脚本补；有的宿主必须把整套拆开复制到用户目录才生效，那就复制，这不是缺陷。

界线在于**脚本只做机器能判死的事**：读参数、拼命令行、抓会话号、按声明的边界核越界。提示词组装、该派谁、分几档、指南文本生成——全是判断，留给主线程。旧 `review.sh` 的 253 行里大头正是生成一份派发指南文本给主线程读，那是提示词工程封进 shell，不搬。

除这三个之外不新增。

**产品层出现宿主的名字就是缺陷**，比占位符没填更好查，所以固化成一道机器检查：空转脚本扫 `mmw/skills/` 与 `mmw/roles/` 每份文件的线上区（线下区允许讲宿主机制成因），命中 `codex`、`cursor`、`droid`、`factory`、`claude code`、`.codex`、`.claude`、`subagent_type`、`allowed-tools` 即失败，适配层那份 `mmw-dispatch` 除外。模型家族名不在禁列——角色文件本来就要钉模型。旧插件里出现过从别家抄过来忘改名的「先读某某宿主插件内的技能」，而正确写法根本不需要占位符：写「先读插件内的技能」就完了，读它的那个代理眼前只有一个插件。

宿主专属的东西照实写，不做兼容分支：`mmw-dispatch` 里那行无头命令就直白写成当前的样子，换宿主时整份换掉，不为「将来可能换后端」预留抽象层。

## 2.6 钩子（1 个，选配）

只剩出站发布一条：推送、远端侧合并、部署命中即弹权限框。本地合并不拦——可逆、不出站，拦它只会打断无人值守的推进。

删掉的两个：记提交进度（改从 git 推导）、会话开场分诊（由用户敲的第一条入口代劳）。红线脚本里那层反规避处理也删——脚本自己的注释已经承认那些是刻意规避、最后防线本就是权限框。

## 2.7 整块不进新插件

| 旧的东西 | 规模 | 为什么不要 |
| --- | --- | --- |
| 四份场景预设文档 | 352 行 | 阶段可跳之后，场景就是「从哪一格开始、跳过哪几格」 |
| 五个控制面命令 | 5 个文件 | 分别等于起审、不敲命令、直接说话、退回计划格、说一句话 |
| 值守档三档合同 | 69 行加五十处判断 | 中间那档没有独立语义，收成两档并进往下走那份技能 |
| 取证战役 | 17 行 | 与查清现状是同一件事同一套方法 |
| 跨计划合同门单列 | 36 行 | 角度并进终审 |
| 两份并行编排脚本 | 272 行 | 并行就是把派发的第一步连着调几次 |
| 共用片段的锚点机器 | 133 行机器加 112 行片段、散在 20 个文件 | 它的主顾是被四个场景文件各抄一份的阶段契约表；一件事一份技能之后没有东西需要复制 |
| 命令层 | 11 个文件 332 行 | 宿主已把命令并入技能 |

## 2.8 四个收敛循环，各自带停止规则

推进引擎删掉之后，**防打转的责任跟着搬家**。旧引擎里那些「同一批缺陷反复出现就停」的守卫随引擎一起没了，这个责任必须由各自的技能带着，否则代理会在一格里无限转。

真正有价值的循环全都发生在单格内部，一根都不受删引擎影响。四个循环长得一模一样——干一轮、看结果、判够不够、不够就再来——区别只在干什么和什么时候算够。

| 循环 | 在哪一格 | 停在哪 | 写在哪份 |
| --- | --- | --- | --- |
| 解雾 | 探路 | 决策清单清空 | `mmw-wayfind` |
| 原型迭代 | 做设计 | 用户认可 | `mmw-prototype` |
| 审查返工 | 任何起审的格 | 没有新的承重发现 / 同一批问题连转两轮 / 同一格审满三轮 | `mmw-review` |
| 打包自愈 | 出包 | 出包成功 / 修复轮次用尽 | `mmw-release` |

停下来不等于失败：把卡在哪写清楚交给用户，比接着转便宜。

## 2.9 引用外部技能的写法

引用一律**只写名字**，让代理按名字去调，各家把技能装在哪儿是各家的事。插件内部技能之间也用名字，不用相对路径——路径既是跳转成本又是路径依赖。**点名的技能不在时不许卡住**：照本格自己的方法做，在回执里说明它不在。

**可以直接按名字引用、没有冲突条款的外部技能**（第四层填正文时用得上，省得自己重写）：测试驱动循环、代码库设计、领域建模、切工单、分诊、追问、改进架构、浏览器操作。

外部技能有冲突条款时不引用，把手艺吸纳过来自己重写，并在自己这份里明写哪一条不采纳。目前已知两处：外部原型技能要求原型进一次性分支（与「原型是设计的证据、随设计提交」冲突），外部诊断技能要求读一个固定文件名的上下文文档（与「按目标仓库的项目指令链读」冲突）。引用一份带冲突条款的技能，会让代理在两套规矩之间犯迷糊，代价高于重写。

## 2.10 已知的宿主缺口

本次只做一家，这些缺口现在都不成立，但迁移时会当场撞上。先记着，不为它们预留抽象层。

| 缺口 | 什么情况 | 撞上时怎么办 |
| --- | --- | --- |
| 模型名的归属 | 有的宿主里模型标识指向**用户自己注册的模型**，不是宿主内置的。插件自带这类标识，换台机器就失效 | 安装脚本去读用户的模型注册表，把「推理最强的一档」翻译成一个真实可用的名字 |
| 谁建任务工作树 | 三家代理自己建自己进；有一家要用户先在界面建，代理只能就地认领 | 开任务那份技能按宿主分岔，不是加兼容分支而是那一家单独一份 |
| 提问用户的工具叫什么 | 四家四个名字 | 报准，否则代理会乱猜 |
| 钩子自己出错时怎么办 | 一家有显式的失败即拦标志、一家靠返回值、一家靠退出码，三家不一致 | 出站红线是唯一的钩子，按那一家的机制写死 |
| **顾问** | Claude Code 原生带这个能力，别家只能用子代理仿一个 | 原生有的直接用原生的，插件不为它造一份；别家怎么仿，等那几家的能力面看清楚再定 |

**顾问是什么，先记准，免得下次照错的定位去建**：它给主线程提供决策指引、帮着发现盲点，不是一个替主线程做三选一裁决的裁判。触发它靠条件不靠节拍——取舍两边接近、证据互相矛盾、或这个选择会锁死难以回头的架构。只有主线程能调，子代理调它会造成嵌套派发、责任链断掉。本次不建。

## 2.11 本层边界

已经定死：新插件的位置与名字、文件清单、每份的来源与保留删除两列、六份角色的参数、产品层与适配层的分界、钩子一条、宿主无关的三条写法纪律、四个循环各自的停止规则。

故意没碰：任何方法论正文（第四层）、状态读写与派发怎么写（第三层）。

---

# 第三层 · 接线

机制接通，方法论仍留空。判据是能空转一整轮：切格、派发、落盘都通，被派者拿到的提示词还是空壳。

验收证据在 `docs/design/mmw-rebuild/dryrun.sh`，跑一遍十二步全绿。起无头进程那步不在脚本内（要花钱），单独实测过。

判「通了」不看主线程读起来顺不顺，看每个角色在自己那一步能不能从文件里答出五个问题：我是什么角色、要读什么、要做什么、要输出什么、输出到哪里。逐个角色逐个交接点核过一遍，查出九处答不上来的，见 3.5。

## 3.1 这一层落在哪几个文件

| 文件 | 接了什么 |
| --- | --- |
| 七份入口技能 | 各加一节「怎么做」，写清读什么、改哪个字段、打印什么 |
| 九份阶段技能 | 各写清自己这一格的产物落在哪、要派谁、派谁时交代什么 |
| 六份角色文件 | 正文写清边界、开工要拿到什么、收工回什么，由派发脚本抄进提示词 |
| `scripts/dispatch.sh` | 实现 `run` / `resume` / `check` / `preview` |
| `hooks/guard-outbound.sh` 与 `hooks.json` | 实现出站红线与它的前筛 |

状态读写没有脚本：五个字段的 JSON，主线程照技能正文读写。

## 3.2 实测定下来的宿主事实

派发后端是 Codex CLI 0.145.0。四条实测结论改了原来的设计：

| 事实 | 影响 |
| --- | --- |
| `--json` 的第一个事件就带会话号 | 不用再 grep 带色码的文本头。旧插件那个「剥 ANSI 才不丢续接记账」的坑，在这个版本里不存在了 |
| `resume` 没有 `--sandbox`、`-C`、`--add-dir` | 那三个是 `exec` 的选项，要放在 `resume` 之前。围栏靠这个顺序钉住，不是靠 `resume` 自己 |
| `--output-schema` 可以钉死返回结构 | 记下来备用，**现在不接**。第四层写返回合同时真有需要再加，没人用的参数不预留 |
| `codex` 的 `[agents.*]` 是它自己派子代理用的 | 外部点不了名，所以派发仍走命令行参数 |

宿主这一侧另一条实测事实：钩子条目的 `if` 前筛已经按 bash 语法分好段——剥前导环境变量赋值、拆 `&&` 与 `;`、连 `$()` 里的也查。旧插件在脚本里自己写的那套解析，宿主层已经有了。

## 3.3 派发脚本

`dispatch.sh` 三个子命令，六份角色都往同一个入口投，调用方只给角色名和这一次的活。

**调用方不必先判断走原生还是无头**：碰到本宿主自家模型的角色，脚本回 `NATIVE=<角色名>` 并退 4，告诉调用方改用宿主的子代理工具、`subagent_type` 填什么。一条命令记到底。

**角色文件的正文与技能名单由脚本自动抄进提示词开头**。无头那一侧看不到角色文件，「你是谁、边界在哪、收工回什么、读哪几份」不抄就一个字都送不到——原生那两个角色靠宿主加载角色文件拿得到，无头那四个拿不到，同一份文件两路行为相反。抄的都是角色文件已写死的事实，机器抄比人抄准；这次干什么才是判断，那部分仍然全由调用方写。

`preview` 子命令只打印会送出去的全文、不派，给这条接线一个能看的面：空转里逐个角色断言四要素齐。

| 子命令 | 干什么 | 出参 |
| --- | --- | --- |
| `run` | 起一个无头进程 | 头两行 `SESSION=` 与 `EXIT=`，之后是最后一条消息 |
| `resume` | 续接追问 | 同上 |
| `check` | 按角色声明的边界核越界 | 干净回 0，越界回 3 并列出文件 |
| `preview` | 只组装不派，看送出去的全文 | 提示词全文 |

会话号不落盘：主线程后台起、从输出里读，少一套记账机制。

**两条前置，缺一当场停，不猜默认值**：角色点名要读的技能必须已装进派发后端（没装就指向 `install.sh`，别让它开工后才发现方法论找不到）；可写角色首派时工作树必须干净。第二条是空转测出来的——工人开工前工作树里若有未提交文件，收工核越界时分不清哪些是它改的。因此开任务的顺序也跟着定死：**先把状态目录加进忽略清单并提交，再建工作树**，否则状态文件本身会以未跟踪身份混进工人的改动。

越界判据来自角色文件：声明 `allow-paths` 就是白名单（计划撰写者只准碰计划与切片），声明 `deny-paths` 就是黑名单（落地工人禁碰文档目录）。

## 3.4 出站红线

只拦三类：推送、远端侧合并、部署。本地合并放行——可逆、不出站，拦它只会打断无人值守的推进。

判法是看子命令位而不是「命令里出现过这个词」：引号里含空白的先摘掉（提交信息、检索模式是数据不是动作），剥掉工具自己的选项后取第一个非选项词。十九条边界用例实测：该拦的十条全中，不该拦的九条全放，包括 `git commit -m "add push handler"` 与 `git merge --no-ff`。

不做反规避层，也不列具名工具清单。刻意绕开的写法一概不追——追下去只会长成一张永远不全的清单，给人虚假的安全感；它挡的是顺手敲出去，不是有意绕过。部署因此只认命令名里带 `deploy` 与参数位上独立的 `deploy` 词，不点名任何具体的部署工具。已知误报面写在脚本头部：heredoc 正文里的推送命令会误弹一次。

## 3.5 五问核出来的九处断口

接线通不通不由主线程的顺手程度决定。逐个角色、逐个交接点问那五个问题，查出九处文件里答不上来，全部补齐：

| 断在哪 | 现象 | 补法 |
| --- | --- | --- |
| 角色说明送不到无头侧 | 派发脚本只读角色文件的 frontmatter，正文一个字不抄。工人的可写边界、开工清单、收工要求只在原生那两个角色上生效 | 脚本把正文也抄进提示词开头，`preview` 给出可观测面 |
| 回执没有结构 | 三处写「回结构化报告」，没有一处说结构是什么 | 各角色正文里逐样写死：工人五样、计划撰写者四样、审者每条四样、探子每条带出处 |
| 产物路径只在一处 | 报现状那份技能列了四条路径，其余各格技能一律没写自己的产物落哪，而切格那份却说「按约定该落在哪」 | 七格各写自己那一行 |
| 探子的产出无处落盘 | 探子只读、写不了盘，没有任何一处说谁把它的回执整理成现状报告、落到哪 | 查现状那份写明由主线程合成落盘，写进去前逐条回源码亲验 |
| `note` 没人写 | 只有无人值守代敲时写，人推进时永远是空的，而报现状那份指望念它 | 每次换格都写：为什么停、下一步等什么 |
| 旁路清单没人写 | 开任务建空文件、收尾摊开它，中间无人往里记 | 定为主线程记；工人回执里那条「撞到的范围外问题」由主线程转记 |
| 续传后没有指向 | 报现状报三样，不含「接着该读哪份技能」 | 补成四样 |
| 派活缺项 | 派发那份给的是几个角色混在一起的清单，派探子时「落哪份计划」不适用，派工人时缺「工作树在哪」 | 指向角色文件各自那行开工清单，逐项填齐 |
| 并行工作树无主 | 落地与写计划两处都说「各一棵工作树」，零处说建在哪、怎么合回 | 路径与合回方式写进派发那份，两处共读 |

## 3.6 本层边界

已经定死：七个入口各自的动作步骤、状态文件的读写时机、九格产物的落盘位置、六份角色的开工清单与回执合同、派发脚本的四个子命令与两条前置、越界判据、出站红线的判法与它的前筛。

故意没碰：任何方法论正文（第四层）。被派者现在拿到的提示词知道自己是谁、边界在哪、收工回什么、该读哪几份技能，那几份技能里还没有方法。

---

# 留到第四层拍板的一处

**出包自愈到底搬多少。** 旧插件那套是 2277 行的脚本（`plugin/scripts/release-flow.sh` 1310 行、`package-phase.sh` 353 行、两份 python 合计 614 行），干的是跑构建阶段、按退出码分级、熔断、失败重生。新插件现在一行都没有，只有 `mmw-release` 与 `mmw-package` 两份技能占位。

拍板要的事实还没拿到：那 2277 行里多少是真正的机器活（跑构建、解析退出码、记轮次、熔断），多少是能写成方法论的判断。填到出包那两份技能时先把它读完，给出「哪些必须留成脚本、哪些改写成方法论」的对照与行数估算，再定。**在那之前不动它，也不假设它会进来。**
