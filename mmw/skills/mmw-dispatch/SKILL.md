---
name: mmw-dispatch
description: 把 roles/ 里的角色派出去干活时读。
user-invocable: false
---

六份角色都往同一个入口投：

| 干什么 | 怎么敲 |
| --- | --- |
| 派一个 | `scripts/dispatch.sh run --role <角色> --cwd <目录> --prompt <文件>` |
| 续接追问 | 同上，`resume` 加 `--session <会话号>` |
| 收工核越界 | 同上，`check` 加 `--since <起点提交>` |

**提示词只写这次的活**。写之前翻 `roles/<角色>.md` 那行「开工要拿到」，逐项填齐——每个角色要的东西不一样，缺一项它就得回头问你。

角色说明、边界、回执要求、该读哪几份技能，脚本从角色文件抄进提示词开头；用什么模型、能不能写盘也由脚本按角色文件钉死。这几样都不用你写。

派原生角色时脚本只回一个角色名，角色文件由宿主自己加载；提示词照同样写法写，开头点名它该读的几份技能（名单在角色文件 `skills` 字段）。

**派可写角色前先给它一棵工作树**：从任务分支分叉出 `.mmw/worktrees/<任务名>-<片名>`。派发脚本要求首派时工作树干净，几个可写角色挤同一棵就会互相顶掉。验收过了 `git merge --no-ff` 合回任务分支，再清掉那棵树。只读角色不用建，就在任务工作树里跑。

**一律起在后台。**并行就是连着起几个，各一棵工作树。

---

## 线下 · 不是技能内容

**这份属于适配层，换一家宿主整份重写。**

**为什么原生角色也要点名**：宿主的角色文件字段只保证被派者拿得到那几份，不保证它去读；无头那一侧连角色文件都看不见。点名是每家宿主都做得到的唯一一件事。

**为什么一律后台**：审一轮、落地一份计划都常超前台等待上限。

### 施工单

- **来源**：`plugin/scripts/worker.sh` 的 `dispatch` / `plan-dispatch` / `resume`、`plugin/scripts/review.sh` 的派发指南
- **保留**：后台起；恢复会话要重钉围栏；提示词纯路由不内联方法论；并行是连着起几个
- **删除**：把派发指南文本生成封进 shell（旧的 253 行大头）；审者分档矩阵；派发前先落盘一份指南再读回来
