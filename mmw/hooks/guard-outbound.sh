#!/usr/bin/env bash
# 唯一的机器硬检查：出站发布。推送、远端侧合并、部署命中即弹权限框，由用户亲批。
# 本地 merge 不拦——可逆、不出站，真红线是它之后的推送；拦它只会打断无人值守的推进。
#
# 施工单
#   来源：plugin/hooks/guard-redline.sh
#   保留：命中即弹框、由平台权限框保证真人批准、本地 merge 放行
#   删除：反规避层（剥 heredoc、抽 eval 内串、引号归一）——脚本自己的注释已承认
#         那些是刻意规避、最后防线本就是权限框；误报由用户点一下解决
#   删除：记提交进度（改从 git 推导）、会话开场分诊（由用户敲的第一条命令代劳）
set -euo pipefail
echo "TODO: 第三层接线时实现" >&2
exit 0
