# 旁路清单

本次任务范围外的东西记这里。谁撞到谁往表里加一行。前四栏是记的人当时看到的事实，别人不改；
后来在别处摸到线索、根因或修法的，往「后来查到的」续一句，句首署上自己在哪个阶段。

| 现象 | 位置 | 为什么这次不做 | 撞到的阶段 | 后来查到的 |
| --- | --- | --- | --- | --- |

<!-- 照这样加行：
| 登录页在 Safari 下回调丢参数 | `web/src/auth/callback.ts:42` | 不属于本次结算改造 | investigate | build：是 SameSite=Lax 拦掉了回跳 cookie，改一行 set-cookie 就好 |
现象一句话说清坏在哪，不写猜测的原因；位置给 `文件:行号` 或链接，给不出就写「未定位」；
理由要说清跟本次任务的边界关系，不写「以后再说」；最后一栏没东西补就留空。
-->
