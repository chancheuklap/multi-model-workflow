# 旁路清单

本次任务范围外的东西记这里。谁撞到谁往表里加一行：不修它、不展开调查、也不动别人加过的行。

| 现象 | 位置 | 为什么这次不做 | 撞到的阶段 |
| --- | --- | --- | --- |

<!-- 照这样加行：
| 登录页在 Safari 下回调丢参数 | `web/src/auth/callback.ts:42` | 不属于本次结算改造 | investigate |
现象一句话说清坏在哪，不写猜测的原因；位置给 `文件:行号` 或链接，给不出就写「未定位」；
理由要说清跟本次任务的边界关系，不写「以后再说」。
-->
