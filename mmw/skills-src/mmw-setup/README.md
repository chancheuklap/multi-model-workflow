# mmw-setup 不是技能

这目录只存插件早期的仓库配置做法，已经被 `mmw init` 取代。

- `mmw skills materialize` 会跳过它。
- 不要按这里的命令跑现役流程。当时的 `mmw task new`、agent 自己建任务树、Wiki 归档、文末「下一步」表都已取消。
- 现役流程以 `mmw/skills-src/` 其他目录、各宿主技能产物和 `mmw --help` 为准。

背景线索从 [legacy-setup.md](legacy-setup.md) 读。
