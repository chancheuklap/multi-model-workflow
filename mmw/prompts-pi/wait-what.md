---
description: "停一下，刚才那个我没看懂；换一种方式再讲一遍，或者画出来。"
---


等一下——我不明白你现在讲到哪一步了。换一种方式重新说明：补一点上下文，使用 ASD-STE100 简化技术英语（Simplified Technical English），并采用领域上下文中的通用语言。

用户没有另行指出时，要重新说明的就是上一条消息。用户点名一份文档时，重新说明的是那份文档。session 里的输出和仓库里的各种文档都在范围内。

在仓库任务中，先遵守目标仓库 `AGENTS.md` 的领域上下文规则，使用领域上下文中的 canonical 术语；没有领域文档时使用行业标准术语。

只重新说明用户点名的内容，不引入新方向。根据读者缺少的上下文解释必要的缩写和代码标识符。

## 选择分支

| 用户需要什么 | 处理 |
| --- | --- |
| 用更简单的文字重新说明 | 补齐理解当前内容所需的上下文，再用短句重新说明 |
| 把它画出来，做成普通 HTML 可视化解释 | 完整读取 下文的「VISUAL.md」 |
| 看不懂的是一套业务逻辑、状态转移或数据形状，需要自己按按钮走一遍才能明白 | 移交 `/mmw-prototype`，交给它当前看不懂的内容、现有状态模型或后端脚本的精确路径，以及要弄明白的问题；它按 Logic HTML 工作面制作可点击的页面 |

## 下一步

| 情况 | 下一步 |
| --- | --- |
| 文字重新说明已经完成 | **停**：展示重新说明后的内容，等待用户反馈 |
| 用户需要按按钮走一遍才能明白 | **移交**：`/mmw-prototype`，按 Logic HTML 工作面制作可点击的页面 |

## VISUAL.md

# 普通 HTML 可视化解释

把原文重新组织成能揭示关系的图、表或交互。根据内容，清楚展示整体结构、关键流程或重要对比。让读者先掌握全局，再定位具体节点和过程。概念或流程仍然抽象时，用真实场景中的具体示例逐步演示。如果一张完整的图或表能讲清内容，就在同一个视图中集中呈现。让读者直接看清全局关系。不要照抄原文，也不要只把原文放进图形框。

本文件把方案、文档或回复解释成 HTML。需要让用户按按钮驱动状态模型才能弄明白时，回 上文的「SKILL.md」 的「选择分支」移交 `/mmw-prototype`。

先完整读取用户要求解释的方案、文档或回复。根据当前对话确定要解释的范围。

可视化解释必须完整覆盖用户要求解释的内容。沿用其中的术语和表达，不创造新术语、缩写或代号。内容涉及仓库时，使用领域上下文中的 canonical 术语；存在冲突时显示冲突。

以图、表和交互为主，只使用必要的标签、短说明和出处。让读者尽量不打开原文也能看懂。

## 路线一：完整 HTML

这是默认路线。生成完整 HTML。要解释的内容本身就是仓库里的一个文件时，HTML 放在它旁边，同名换成 `.html`。要解释的内容只存在于这段对话里时，问用户想保存到哪个目录，然后把这段内容和 HTML 一起放进去。

宿主有把本地 HTML 发布成可访问网页的能力时，用它发布，发布出去的内容与本地 HTML 一致；发布成功后把网址写进本地 HTML 同目录的 `<同名>.url` 里，一行一个网址——同一份本地 HTML 以后再更新时，读它拿到上次的站点，复用那个站点存成新的一版。完成后交回本地路径和网址。宿主没有这个能力时，本地 HTML 就是最终产物。

这条路线完成时，本地 HTML 已保存并能打开。宿主支持发布的，站点上已经有本次更新的新版本；发布失败或者宿主根本不支持发布的，保留本地 HTML 并说清网页这一步没有做——不要说成做了。

## 路线二：对话内可视化

只有用户要求解释的范围非常精确、非常小，而且一个紧凑的图或表可以完整讲清时，才使用这条路线。

宿主能在对话里直接渲染 HTML fragment 时，用一个 fragment 把这个小范围完整讲清。宿主渲染不了时这条路线不成立——回路线一出完整 HTML，不要把 fragment 的源码当文本贴出来充数。

这条路线完成时，HTML fragment 已经在对话内显示出来，并完整覆盖用户要求解释的范围。

## 下一步

| 情况 | 下一步 |
| --- | --- |
| 完整 HTML 已在本地保存，而且有调用方等待 | **移交**：把本地 HTML 路径交回调用方；宿主支持发布的，连网址一起交 |
| 本地 HTML 已保存但网页没发出去，而且有调用方等待 | **移交**：交回本地 HTML 路径，并说清网页这一步没做 |
| 小范围内容已经在对话内完整显示，而且有调用方等待 | **移交**：把对话内可视化结果交回调用方 |
| 完整 HTML 或对话内可视化已经完成，而且没有调用方等待 | **停**：展示可视化并说明本地文件位置或对话内结果 |
| 本地 HTML 已保存但网页没发出去，而且没有调用方等待 | **停**：展示本地产物并说清网页这一步没做 |
