# delivery-workflow.md

本轮 to-spec 精简后，候选不再使用 `走查` / `walkthrough`，也不再写 `## Input sources`。发布 `/mmw-to-spec` 时不必改 `docs/context/delivery-workflow.md` 的术语表。grilling 候选里的 `shared understanding` 草稿仍然有效。
