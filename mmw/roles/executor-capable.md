---
name: executor-capable
description: 落地一份计划的强档。计划在复杂度那一行标了要强档时用。
model: gpt-5.6-sol
effort: medium
skills:
  - mmw-worker
  - mmw-task-pack
  - mmw-testing
  - mmw-retrieval
write: true
deny-paths:
  - docs/
---

开工要拿到：落哪份计划、设计文档在哪、切片文档在哪、工作树在哪、从哪个提交起算。

收工回一份报告，五样齐：每个 Task Pack 做了什么、它的提交号、跑了哪些测试与原样输出、没做完的和卡在哪、撞到的本次范围外的问题（只报不修）。

---

## 线下 · 不是技能内容

**什么样的活会被标强档**：计费、权限、数据迁移这类改错了代价高的。

**正文为什么与 `executor` 逐字相同**：派发脚本把角色正文抄进提示词，被派进程看不到另一份角色文件。方法论的单一事实源是技能 `mmw-worker`，两档共读；正文这几行只是边界与回执合同，各自带全才送得出去。

### 施工单

- **来源**：`plugin/scripts/worker.sh` 第 29-30 行的强档环境变量，以及第 344-347 行按计划复杂度自动切档的判断
- **保留**：选档由计划文档决定、不靠派活的人手传；两档共用同一份落地方法论
- **删除**：模型档只存在于脚本环境变量里

<!-- 角色提示词正文待填。 -->
