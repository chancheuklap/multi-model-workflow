# 已退役技能

`mmw-start` 和 `mmw-closing` 于 2026-08-16 从发布面移出。

`mmw-start` 曾是统一路由入口。各技能的 slash 与 `description` 已经承担路由；任务分支由 `[[mmw-require-task-branch]]` 确认。

`mmw-closing` 曾清理过程材料并宣布分支就绪待集成。spec 与 plan 长期留在仓库（ADR `0001`）。过程材料在 gitignore 里，工作树拆掉即消失。implement 出口改为问：出包，还是到这里停。

本目录是冻结归档。不要物化，不要装回宿主。
