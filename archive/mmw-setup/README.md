# 早期仓库配置做法

这目录存插件早期的仓库配置做法，已经被 `mmw init` 取代。它从来不是技能。

2026-08-18 从 `mmw/skills-src/` 移到这里。留在技能源目录期间，扫描技能正文的四道机械校验各自要为它写一处排除；移出之后那四处排除全部删除。

不要按这里的命令跑现役流程。当时的 `mmw task new`、agent 自己建任务树、Wiki 归档、文末「下一步」表都已取消。

现役流程以 `mmw/skills-src/` 和 `mmw --help` 为准。背景线索从 [legacy-setup.md](legacy-setup.md) 读。

本目录是冻结归档。不要装回宿主。
