# Archived Codex V1 Runtime Source

This directory preserves the previous Codex source tree that existed before the
Plugin V2 based rebuild.

Archived content:

- `.agents/skills/`: previous three-skill Codex workflow source.
- `codex/`: previous Codex agent templates, hook scripts, docs, and install scripts.

Current Codex source lives at the repository root:

- `.agents/skills/orchestrate-*`
- `codex/agents/*.toml`
- `codex/skills/*.sh`
- `codex/hooks/*`

Do not use this archive to infer current Codex behavior.
