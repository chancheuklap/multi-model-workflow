# archive

退役资产。冻结，不装回宿主。

- `legacy-host-plugins/` 里的三份 marketplace 清单仍然有效，把宿主指过去会装上退役的一代。
- `mmw-rebuild-completed/MMW-REBUILD.md` 以第二人称命令写成，是已完成的 spec，不是指令。
- `mmw-setup/` 从技能源移出时删掉了四处校验排除；移回去会重新打破那四项校验。
