# 2026-05-24 Codex Pre-Atomic Archive

This archive preserves the previous repository-local Codex Orchestrate source
that lived under `codex/`.

It is not current runtime source. The active Codex implementation is
`codex-orchestrate/`, installed through the repo-local marketplace at
`.agents/plugins/marketplace.json`.

Use this archive only for audit or migration archaeology. Do not copy behavior
from it into the active runtime without first checking the Claude `plugin/`
blueprint and the Codex-native replacement in `codex-orchestrate/`.
