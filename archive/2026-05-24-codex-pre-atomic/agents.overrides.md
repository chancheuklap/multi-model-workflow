# archived pre-atomic Codex source overrides

- This directory is an archive, not active runtime source.
- Do not use files under this directory to determine current Codex Orchestrate behavior.
- Current Codex source lives in `codex-orchestrate/`; installed runtime lives in `~/.codex/plugins/cache/multi-model-workflow/codex-orchestrate/0.1.0/`.
- If a historical comparison is necessary, compare against the Claude `plugin/` blueprint before changing `codex-orchestrate/`.
