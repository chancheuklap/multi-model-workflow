# archived codex/skills overrides

These skill installers are archived legacy material. Do not reinstall
`.agents/skills/orchestrate-*` from here.

Current skill source lives in `codex-orchestrate/skills/` and is loaded through
the installed `codex-orchestrate` plugin.
