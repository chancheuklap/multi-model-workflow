# Archived Agent Templates

These TOML files are the old pre-atomic custom-agent templates. They are kept
for historical comparison only.

Current agent templates live in `codex-orchestrate/agents/` and are synced to
`~/.codex/agents/` by `codex-orchestrate/installers/install.sh`.
