# archived codex/hooks overrides

These hooks are archived legacy material. They are not installed or registered
as the current Codex Orchestrate runtime.

Current hook source lives in `codex-orchestrate/hooks/`. Runtime hook behavior
comes from the installed `codex-orchestrate` plugin cache.
