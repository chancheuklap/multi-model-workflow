# TESTING.md（本仓库薄层）

> 通用测试方法在 `mmw-tdd/SKILL.md`、`mmw-tdd/tests.md` 和 `mmw-tdd/mocking.md`。本文记录本仓库的目录、分层、外部边界和运行命令。两者冲突时点名具体冲突，不静默覆盖。

## 目录分层

写清楚这个仓库的测试分几层、各自放在哪、各自测什么。一行一层。

| 层 | 位置 | 测什么 |
| --- | --- | --- |
| <例如：单元> | <例如：`tests/unit/`> | <这一层守的是哪一类行为> |
| <例如：集成> | <例如：`tests/integration/`> | <这一层守的是哪一类行为> |

## 外部 seam

`mmw-tdd/mocking.md` 说只在系统边界上 mock。在这里列出本仓库已确认的外部边界和默认替代方式。新增边界时同步补充本节。

- <例如：支付服务商的 HTTP 接口——用录制下来的响应>
- <例如：系统时间——测试里定死>

## 权威源指针

测试需要对项目已持有的价格、文案、枚举等值作断言时，在这里指明它的权威源，避免在测试中复制第二份。

| 值 | 权威源 |
| --- | --- |
| <例如：套餐价格> | <例如：`config/pricing.yaml`> |

## 怎么跑

整套测试的命令，加上提交之前必须过的那些检查。

```bash
<例如：uv run pytest>
```

## 这个仓库特有的边界

目录分层、外部 seam、权威源指针、怎么跑这四节装不下、但写测试的人必须知道的约定写在这里。没有就删掉本节。
